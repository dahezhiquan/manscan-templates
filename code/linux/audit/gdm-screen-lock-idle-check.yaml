id: gdm-screen-lock-idle-check

info:
  name: 确保用户在空闲时 GDM 屏幕锁定（本地基线扫描）
  author: Th3l0newolf
  severity: medium
  description: |-
    此模板检查是否配置了在一段用户无操作时间后锁定屏幕。
  remediation: |-
    根据组织的安全策略配置屏幕锁定设置：
      - 将 `lock-delay` 设置为 5 秒或更短。
      - 将 `idle-delay` 设置为 900 秒（15 分钟）或更短，并确保其未被禁用。
  reference:
    - https://www.cisecurity.org/benchmark/ubuntu_linux
  tags: cis,linux,ubuntu,gdm,screensaver,screenlock,audit
  metadata:
    verified: true

self-contained: true

code:
  - engine:
      - bash
    args:
      - "-c"
      - |
        lock_delay=$(gsettings get org.gnome.desktop.screensaver lock-delay 2>/dev/null | awk '{print $2}')
        idle_delay=$(gsettings get org.gnome.desktop.session idle-delay 2>/dev/null | awk '{print $2}')

        if [ -n "$lock_delay" ] && [ -n "$idle_delay" ] && \
           [ "$lock_delay" -le 5 ] && \
           [ "$idle_delay" -gt 0 ] && \
           [ "$idle_delay" -le 900 ]; then
          echo "[gdm-screen-lock-idle-check:Policy-Pass] [Screen lock is configured correctly] [CIS_PASS] [medium]"
        else
          echo "[gdm-screen-lock-idle-check:Policy-Fail] [Screen lock is not configured correctly] [CIS_FAIL] [medium]"
        fi

    matchers:
      - type: word
        name: policy-pass
        words:
          - "Policy-Pass"

      - type: word
        name: policy-fail
        words:
          - "Policy-Fail"
# digest: 4a0a00473045022100be8d82fc5c709641b04535b1b34c9e69db9602eb06a0e4b508c1c7dc598e2a30022065d4f36251f33969b7c7cac33ce18f1b0115cd6ff96f514b5f5f23e57064e639:922c64590222798bb761d5b6d8e72950
