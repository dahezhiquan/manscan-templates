id: ibm-api-connect-panel

info:
  name: IBM API Connect 面板识别（指纹识别）
  author: righettod
  severity: info
  description: 该模板通过识别 IBM API Connect 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  reference:
    - https://www.ibm.com/products/api-connect/developer-portal
  metadata:
    max-request: 1
  tags: panel,ibm,api,detect,login,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}/auth/manager/sign-in/'
      - '{{BaseURL}}'

    stop-at-first-match: true
    host-redirects: true
    max-redirects: 2
    matchers-condition: and
    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200 || status_code == 404'

      - type: dsl
        dsl:
          - 'contains_any(to_lower(body), "content=\"ibm api connect", "ibm api connect</title>")'
          - 'contains_all(to_lower(body), "window.apiConnectCfg", "ibm-cloud", "<title>api connect")'
        condition: or
# digest: 490a004630440220727318837ae1a7a78d2ba84344e01fec5ee2133de4d1c321f6457f5e6ad5b5340220791cf535d141871303bad10a5cee34131d040a5352c60f5533410910429fc33f:922c64590222798bb761d5b6d8e72950