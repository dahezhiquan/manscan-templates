id: user-registration

info:
  name: WordPress User Registration & Membership Plugin 指纹识别（指纹识别）
  author: omarkurt
  severity: info
  description: |
    用于识别目标是否部署 WordPress User Registration & Membership Plugin 并提取相关版本或构建信息，模板会结合页面正文、状态码、特定路径或已知端点和版本或标识提取结果进行指纹匹配。
  reference:
    - https://wordpress.org/plugins/user-registration/
  metadata:
    max-request: 1
    verified: true
    shodan-query: "/wp-content/plugins/user-registration/"
  tags: wordpress,wp-plugin,user-registration,tech,wp

http:
  - method: GET
    path:
      - "{{BaseURL}}/wp-content/plugins/user-registration/readme.txt"

    matchers-condition: and
    matchers:
      - type: word
        words:
          - "User Registration"
        part: body

      - type: regex
        part: body
        regex:
          - '(?i)Stable tag:\s*[\d.]+'

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        name: version
        group: 1
        regex:
          - '(?i)Stable tag:\s*([\d.]+)'

      - type: dsl
        dsl:
          - '"[User Registration] detected on " + host + " (version: " + version + ")"'
# digest: 4a0a00473045022010476814bc9d15af92d7922e505f6a73181f56caf2da8aef8904fbb5de0bcfc9022100831fa63d909b336975982ac1544c44ccc47c547d670793b647be5fd429032f04:922c64590222798bb761d5b6d8e72950