id: asyncapi-inventory

info:
  name: AsyncAPI Spec Inventory 暴露（暴露面识别）
  author: hamzasahin61
  severity: info
  description: |
    检测到对外可访问的 AsyncAPI Spec Inventory。公开的接口文档、调试端点或管理 API 可能泄露路由结构、参数格式、鉴权方式和内部功能边界，从而帮助攻击者快速完成接口枚举与利用准备。
  reference:
    - https://www.asyncapi.com/docs/reference/specification
    - https://raw.githubusercontent.com/asyncapi/spec/v2.6.0/examples/streetlights-kafka.yml
  tags: exposure,asyncapi,api,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/asyncapi"
      - "{{BaseURL}}/asyncapi.yaml"
      - "{{BaseURL}}/asyncapi.yml"
      - "{{BaseURL}}/asyncapi.json"
      - "{{BaseURL}}/api/asyncapi"
      - "{{BaseURL}}/api/docs/asyncapi"
      - "{{BaseURL}}/docs/asyncapi"

    redirects: true
    max-redirects: 2
    stop-at-first-match: true

    matchers-condition: and
    matchers:
      - type: regex
        part: body
        regex:
          - (?i)\basyncapi\s*:\s*['"]?[0-9.]+
          - (?i)"asyncapi"\s*:\s*"([0-9.]+)"
        condition: or

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        name: version
        part: body
        group: 1
        regex:
          - (?i)\basyncapi\s*:\s*['"]?([0-9.]+)
          - (?i)"asyncapi"\s*:\s*"([0-9.]+)"
# digest: 490a0046304402206356c676a97900b5be09ad5fa480b1fc9ed5ac80af3d6030478c691ad02e4d9e022024ba0c072769840d52b810ed2a9950f1a72a40ff2e222ee833fc47cb224d1563:922c64590222798bb761d5b6d8e72950