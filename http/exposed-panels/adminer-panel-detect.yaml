id: adminer-panel-detect

info:
  name: Adminer 登录面板识别（暴露面识别）
  author: random_robbie,meme-lord
  severity: info
  description: 该模板通过识别 Adminer 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  reference:
    - https://blog.sorcery.ie/posts/adminer/
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
    cpe: cpe:2.3:a:adminer:adminer:*:*:*:*:*:*:*:*
  metadata:
    max-request: 741
    vendor: adminer
    product: adminer
    shodan-query:
      - cpe:"cpe:2.3:a:adminer:adminer"
      - http.title:"login - adminer"
    fofa-query:
      - title="login - adminer"
      - app="adminer" && body="4.7.8"
    hunter-query: app.name="adminer"&&web.body="4.7.8"
    google-query: intitle:"login - adminer"
  tags: panel,fuzz,adminer,login,sqli,discovery

http:
  - raw:
      - |
        GET {{path}} HTTP/1.1
        Host: {{Hostname}}
        Accept: application/json, text/plain, */*
        Referer: {{BaseURL}}

    payloads:
      path: helpers/wordlists/adminer-paths.txt

    threads: 50
    stop-at-first-match: true

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains(body, "System<td>")'
          - 'contains_any(body, "- Adminer</title>>", "Adminer</a>")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '<span class="version">([0-9.]+)'
# digest: 490a0046304402206dd4fc0206f66a5045afc28d922f887c17597f3a9375e417cd49f35061538517022022cda9cbfb4704b6f11c337c32b19061659bdcab49d85ccb29aa1be0c0dce0b0:922c64590222798bb761d5b6d8e72950