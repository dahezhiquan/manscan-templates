id: ibm-api-connect-detect

info:
  name: IBM API Connect Developer Portal 指纹识别（指纹识别）
  author: righettod
  severity: info
  description: 用于识别目标是否部署 IBM API Connect Developer Portal 并提取相关版本或构建信息，模板会结合 favicon 哈希、页面正文、特定路径或已知端点和版本或标识提取结果进行指纹匹配。
  reference:
    - https://github.com/ibm-apiconnect/devportal
    - https://www.ibm.com/products/api-connect/developer-portal
  metadata:
    verified: true
    max-request: 3
    shodan-query: http.favicon.hash:"801517258"
  tags: tech,ibm,detect,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}'
      - '{{BaseURL}}/favicon.ico'
      - '{{BaseURL}}/modules/ibm_apim/ibm_apim.info.yml'

    stop-at-first-match: true
    matchers:
      - type: dsl
        dsl:
          - 'contains_any(to_lower(body), "ibm developer portal is ready", "package: ibm api developer")'
          - '("801517258" == mmh3(base64_py(body)))'
        condition: or

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)version:\s+([0-9a-z\-_.]+)'
# digest: 4b0a0048304602210085df78f0db113b32422c2af3c17d22dd9b2dc667e5cdd321358522d22c610278022100a91a39e037ff28dafc6b4014974f1ee848bded97bf09933b278fc725198f1856:922c64590222798bb761d5b6d8e72950