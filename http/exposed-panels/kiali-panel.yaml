id: kiali-panel

info:
  name: Kiali 识别（暴露面识别）
  author: righettod
  severity: info
  description: |
    该模板通过识别 Kiali 面板、控制台或管理界面的页面特征，判断目标是否暴露了对应服务入口。若命中，说明该后台或运维界面对外可访问，可用于资产识别与暴露面排查。
  reference:
    - https://kiali.io/
  classification:
    cpe: cpe:2.3:a:kiali:kiali:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 2
    vendor: kiali
    product: kiali
    shodan-query: title:"Kiali"
  tags: panel,kiali,detect,login,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/kiali/api/status"
      - "{{BaseURL}}/kiali/"

    host-redirects: true
    max-redirects: 2
    stop-at-first-match: true
    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_any(to_lower(body), "kiali-ui", "<title>kiali</title>", "<title>kiali console</title>", "kiali version")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)"Kiali version":\s*"([a-z0-9.]+)"'
# digest: 490a0046304402204832bfb6426aaaaefe2dd7e8990f58faf318cae1771fc1126dd2ff21105d8b50022040618ef1a308be6a6704f4820d86e99ccc0fe2080a7154cc9fdcb004ad012dd3:922c64590222798bb761d5b6d8e72950