id: apache-ozone-conf

info:
  name: Apache Ozone 暴露（暴露面识别）
  author: icarot
  severity: info
  description: |
    检测到对外可访问的 Apache Ozone。这类配置资源可能包含部署细节、访问凭据、内部地址、组件版本或调试开关，公开暴露后会显著增加被侦察和后续利用的风险。
  classification:
    cpe: cpe:2.3:a:apache:ozone:-:*:*:*:*:*:*:*
  metadata:
    max-request: 1
    vendor: apache
    product: ozone
    shodan-query: title:"Apache Ozone"
  tags: tech,ozone,apache,detect,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}/conf"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<source>ozone-default.xml</source>'
          - 'ozone'
        condition: and

      - type: word
        part: content_type
        words:
          - 'text/xml'

      - type: status
        status:
          - 200
# digest: 4a0a0047304502201eeb047b0a32bd8968ff761dd0faad06582db051d0368a8b1953534b2984cbe302210080d9783d680fb14c74b18ade099a8901fa3b47366fffc55802ba18e7832f8f2e:922c64590222798bb761d5b6d8e72950