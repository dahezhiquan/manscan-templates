id: localstack-detect

info:
  name: LocalStack 指纹识别（指纹识别）
  author: ChrisJr404
  severity: info
  description: |
    用于识别目标是否部署 LocalStack 并提取相关版本或构建信息，模板会结合页面正文、特定路径或已知端点和版本或标识提取结果进行指纹匹配。
  reference:
    - https://github.com/localstack/localstack
    - https://docs.localstack.cloud/references/internal-endpoints/
  metadata:
    verified: true
    max-request: 1
    vendor: localstack
    product: localstack
    shodan-query: '"localstack"'
    fofa-query: 'body="LocalStack"'
  tags: localstack,aws,cloud,detect,discovery,tech

http:
  - method: GET
    path:
      - "{{BaseURL}}/_localstack/health"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains(body, "\"services\"")'
          - 'contains(body, "\"edition\"") || contains(body, "\"version\"")'
          - 'contains(to_lower(header), "application/json")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '"version"\s*:\s*"([^"]+)"'
          - '"edition"\s*:\s*"([^"]+)"'
# digest: 4b0a00483046022100fc1aaa32d7824e1fe74667d1e0b15dc10aa787cbee2099b9a94893c6d0af1409022100d816c8f26759f33254aa7068fe333cbfd50ebb8b6955edc2c244cc725d191070:922c64590222798bb761d5b6d8e72950