id: openwebui-panel

info:
  name: Openweb UI 面板识别（暴露面识别）
  author: rxerium,righettod
  severity: info
  description: |
    该模板通过识别 Openweb UI 面板、控制台或管理界面的页面特征，判断目标是否暴露了对应服务入口。若命中，说明该后台或运维界面对外可访问，可用于资产识别与暴露面排查。
  reference:
    - https://openwebui.com/
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.favicon.hash:-286484075 http.title:"Open WebUI"
  tags: panel,openwebui,login,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/api/config"
      - "{{BaseURL}}/auth"
      - "{{BaseURL}}/opensearch.xml"

    host-redirects: true
    max-redirects: 2

    stop-at-first-match: true
    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_any(to_lower(body), "<title>open webui</title>", "\"name\":\"open webui\"", "<shortname>open webui</shortname>")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '"version"\s*:\s*"([0-9\.]+)"'
# digest: 4a0a004730450220328f59d9746dd330aeb3f7b182f6588bf1394e14db521c6b14373b2714d55610022100a25382a8397b9ca9f189c89ce4d0d979fc06726d9cf76b12251ae672fc139b04:922c64590222798bb761d5b6d8e72950