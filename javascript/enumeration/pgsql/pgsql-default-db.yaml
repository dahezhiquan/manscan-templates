id: pgsql-default-db

info:
  name: PostgreSQL 默认数据库枚举（服务漏洞）
  author: pussycat0x
  severity: high
  description: |
    该模板枚举 PostgreSQL 默认数据库可访问性，用于识别默认实例暴露情况。
  reference:
    - https://github.com/swisskyrepo/PayloadsAllTheThings/blob/master/SQL%20Injection/PostgreSQL%20Injection.md#postgresql-database-name
  metadata:
    verified: true
    max-request: 40
    shodan-query: "product:\"PostgreSQL\""
  tags: js,network,postgresql,authenticated,enum,discovery

javascript:
  - pre-condition: |
      isPortOpen(Host,Port);
    code: |
      const postgres = require('nuclei/postgres');
      const client = new postgres.PGClient;
      connected = client.ConnectWithDB(Host, Port, User, Pass, Db);
      connected  ;

    args:
      Host: "{{Host}}"
      Port: 5432
      User: "{{usernames}}"
      Pass: "{{password}}"
      Db: "{{database}}"

    payloads:
      usernames:
        - postgres
        - admin
      password:
        - postgres
        -
        - 123
        - amber
      database:
        - foresight
        - postgres
        - template0
        - template1
        - test

    attack: clusterbomb

    matchers:
      - type: dsl
        dsl:
          - "success == true"
          - "response == true"
        condition: and
# digest: 4b0a00483046022100c46f3bda20dc3fcfa66ebfe6bf157ded278acbf64a81f8ff33fe01a1a646bcb3022100d9d6f8f668a5fc92c90a8a7c30fc452c42ae422f13aba73928b73226bf28f8b1:922c64590222798bb761d5b6d8e72950