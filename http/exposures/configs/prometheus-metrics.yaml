id: prometheus-metrics

info:
  name: Prometheus Metrics 暴露检测（暴露面识别）
  author: dhiyaneshDK,philippedelteil
  severity: medium
  description: 检测到对外可访问的 Prometheus Metrics。这类配置资源可能包含部署细节、访问凭据、内部地址、组件版本或调试开关，公开暴露后会显著增加被侦察和后续利用的风险。
  reference:
    - https://github.com/prometheus/prometheus
    - https://hackerone.com/reports/1026196
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N
    cvss-score: 5.3
    cwe-id: CWE-200
  metadata:
    max-request: 2
  tags: exposure,prometheus,hackerone,config,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}/metrics"
      - "{{BaseURL}}/api/metrics"

    stop-at-first-match: true

    matchers-condition: and
    matchers:
      - type: word
        words:
          - 'cpu_seconds_total'
          - 'http_request_duration_seconds'
          - 'process_virtual_memory_bytes'
          - 'process_start_time_seconds'
        condition: or

      - type: word
        part: body
        words:
          - "lvm_"
          - "kube"
          - "namedprocess"
          - "mysqld"
          - "collectd_"
          - "libvirt_"
          - "mongodb"
          - "blackbox_"
          - "haproxy_"
          - "rabbitmq_"
        negative: true

      - type: status
        status:
          - 200
# digest: 490a00463044022045c7abdb0dbdcd2cff714e5b9586455be3d0dff18eefe17d939a3be39f8e70d702202020de98f1f94163e12416dd96224bf7112036e3a473d85b582ec8c4ff2c3241:922c64590222798bb761d5b6d8e72950