id: puppetboard-panel

info:
  name: Puppetboard 面板识别（暴露面识别）
  author: c-sh0,daffainfo
  severity: info
  description: 该模板通过识别 Puppetboard 面板、控制台或管理界面的页面特征，判断目标是否暴露了对应服务入口。若命中，说明该后台或运维界面对外可访问，可用于资产识别与暴露面排查。
  reference:
    - https://github.com/voxpupuli/puppetboard
  classification:
    cvss-metrics: CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.title:"Puppetboard"
  tags: panel,puppet,exposure,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<a href="https://github.com/voxpupuli/puppetboard" target="_blank">'
          - 'puppetboard.css" rel="stylesheet"'
        condition: and

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '<a href="https://github.com/voxpupuli/puppetboard" target="_blank">([0-9.]+)</a></div>'
# digest: 490a00463044022010042b9f2a845fde9d9ab8a8c1e09c862e76b2680e592d58369622ea6739c83902205050eee801b0fdb58342185694f00b8513e34c4f6a00b8abe741ee3c3c8ef532:922c64590222798bb761d5b6d8e72950