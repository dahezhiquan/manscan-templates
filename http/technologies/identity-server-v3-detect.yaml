id: identity-server-v3-detect

info:
  name: Identity 服务器 V3 指纹识别（指纹识别）
  author: righettod
  severity: info
  description: |
    用于识别目标是否部署 Identity 服务器 V3 并提取相关版本或构建信息，模板会结合页面标题、页面正文、特定路径或已知端点和版本或标识提取结果进行指纹匹配。
  reference:
    - https://identityserver.github.io/Documentation/
  classification:
    cpe: cpe:2.3:a:wso2:identity_server:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: wso2
    product: identity_server
    shodan-query: http.title:"IdentityServer v3"
  tags: tech,identityserver,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    host-redirects: true
    max-redirects: 2
    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains(body, "IdentityServer v3") && contains(body, "identityserver.github.io")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)build\s+([a-z0-9.-]+)'
# digest: 490a0046304402200dc53daace9ded2e829218937524a8aecb6e48a865b4e8a9fb086e54d3c3b02902204de45e0e29c448455fc811bfa890950e48ceba3c91d6a18b1a5dd3aff92d188d:922c64590222798bb761d5b6d8e72950