id: jakefile-disclosure

info:
  name: Jakefile Build 配置 泄露（暴露面识别）
  author: 0x_Akoko
  severity: info
  description: |
    检测到对外可访问的 Jakefile Build 配置。这类配置资源可能包含部署细节、访问凭据、内部地址、组件版本或调试开关，公开暴露后会显著增加被侦察和后续利用的风险。
  reference:
    - https://jakejs.com/
    - https://github.com/jakejs/jake
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N
    cvss-score: 5.3
    cwe-id: CWE-200
  metadata:
    verified: true
    max-request: 2
    shodan-query: html:"Jakefile"
  tags: exposure,devops,jakefile,nodejs,config

http:
  - method: GET
    path:
      - "{{BaseURL}}/Jakefile"
      - "{{BaseURL}}/Jakefile.js"

    stop-at-first-match: true
    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - '!contains_any(body, "<!DOCTYPE", "<html", "<HTML")'
          - 'contains(body, "task(") && contains_any(body, "desc(", "jake.", "namespace(")'
        condition: and
# digest: 4b0a00483046022100bd79f8b93de317af4083101fb4fcf0d3915f0da0742c496f9e6fcdac34b22f9a022100e5f85665cc94287894f9f299bfd9462e310174c0c87eae5a0bf40dc35f6277d6:922c64590222798bb761d5b6d8e72950