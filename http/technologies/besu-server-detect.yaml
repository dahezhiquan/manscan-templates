id: besu-server-detect

info:
  name: Besu JSON-RPC HTTP 服务器指纹识别（指纹识别）
  author: Nullfuzz
  severity: info
  description: |
    用于识别目标是否部署 Besu JSON-RPC HTTP 服务器 并提取相关版本或构建信息，模板会结合页面正文、特定路径或已知端点和版本或标识提取结果进行指纹匹配。
  reference:
    - https://besu.hyperledger.org/
    - https://besu.hyperledger.org/public-networks/how-to/use-besu-api#service-ports
  metadata:
    verified: true
    max-request: 1
    shodan-query: product:"besu"
  tags: tech,besu,ethereum,web3,blockchain,discovery

http:
  - raw:
      - |
        POST / HTTP/1.1
        Host: {{Hostname}}
        Content-Type: application/json

        {"method":"web3_clientVersion","params":[],"id":1,"jsonrpc":"2.0"}

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains(header, "application/json")'
          - 'contains(body, "besu")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(v[0-9.]+)'
# digest: 4a0a0047304502205099b16c8704c567e9925d880135f4ff3904134f4bcbec1f16cdd8e5ebca2e92022100a6a298ad0405c9a9b7af1a90cb20ac036fb862113b85056a6be9b2e35f727e9d:922c64590222798bb761d5b6d8e72950