id: activemq-openwire-transport-detect

info:
  name: ActiveMQ OpenWire 传输 识别（指纹识别）
  author: pussycat0x,ret2src
  severity: info
  description: |
    该模板通过网络响应特征识别目标是否开放 ActiveMQ OpenWire Transport 相关服务。
  metadata:
    verified: true
    max-request: 1
    shodan-query: product:"ActiveMQ OpenWire transport"
  tags: network,activemq,detect,openwire,detection,tcp,discovery
tcp:
  - inputs:
      - data: "VERSION"

    host:
      - "{{Hostname}}"
    port: 61616,6835

    matchers-condition: and
    matchers:
      - type: word
        words:
          - "ActiveMQ"

    extractors:
      - type: regex
        regex:
          - "ProviderVersion...([0-9.]+)"
# digest: 4a0a0047304502203a10e41dc810fb89413df8dca2d817c0e68ed68d39f3cc717c0991b3b0e4e1ea0221008f6aadc05c66f6b1ce815499a0850ce15ed5f9305427c94f42b4e7c3d56431ae:922c64590222798bb761d5b6d8e72950