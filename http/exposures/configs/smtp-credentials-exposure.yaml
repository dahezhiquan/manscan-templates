id: smtp-credentials-exposure

info:
  name: SMTP 凭据 暴露 检测（暴露面识别）
  author: pussycat0x
  severity: high
  description: |
    检测到对外可访问的 SMTP 凭据 暴露。这类配置资源可能包含部署细节、访问凭据、内部地址、组件版本或调试开关，公开暴露后会显著增加被侦察和后续利用的风险。
  metadata:
    verified: true
    shodan-query: html:"smtp_password"
    max-request: 1
  tags: exposure,smtp,config,credentials

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    host-redirects: true
    max-redirects: 2

    matchers:
      - type: dsl
        dsl:
          - contains_all(body, "smtp_username","smtp_password")
          - status_code == 200
        condition: and

    extractors:
      - type: regex
        part: body
        name: username
        group: 1
        regex:
          - smtp_username":".*"
          - <smtp_username>(.*)</smtp_username>
        internal: true

      - type: regex
        part: body
        name: password
        group: 1
        regex:
          - smtp_password":".*"
          - <smtp_password>(.*)</smtp_password>
        internal: true

      - type: dsl
        dsl:
          - '"Username: "+ username'
          - '"PASSWORD: "+ password'
# digest: 490a0046304402202cb049b5f52df354b3b8a92dc1aed3f15fd5cf8c7f9cb6c48f16e7cdde944969022009e24315e10743321e449ea7af881f5015c80179ab277ccd1fbe0b4da208a86e:922c64590222798bb761d5b6d8e72950