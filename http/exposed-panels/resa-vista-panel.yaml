id: resa-vista-panel

info:
  name: RESA Vista 面板识别（指纹识别）
  author: righettod
  severity: info
  description: |
    该模板通过识别 RESA Vista 面板、控制台或管理界面的页面特征，判断目标是否暴露了对应服务入口。若命中，说明该后台或运维界面对外可访问，可用于资产识别与暴露面排查。
  reference:
    - https://resa.aero/en/solutions-operations-billing/vista-staff-fids/
  metadata:
    verified: true
    max-request: 1
  tags: panel,resa,login,detect

http:
  - method: GET
    path:
      - "{{BaseURL}}/account/login"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_all(to_lower(body), "vista", "resa airport", "login")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - 'v=([0-9.]+)'
# digest: 4b0a0048304602210086c5cc73a1fbce8082a9c94b49c32a82e981b459141c34511f4fdd20ad38b3e8022100ce430c31011edbbd2d245c85c8bafb6928510a801ad257d7e29ff792c8fbd4c3:922c64590222798bb761d5b6d8e72950