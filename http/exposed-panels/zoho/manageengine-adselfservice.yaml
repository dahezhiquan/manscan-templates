id: manageengine-adselfservice

info:
  name: ZOHO ManageEngine ADSelfService Plus 识别（暴露面识别）
  author: dhiyaneshDK,SaK1
  severity: info
  description: 该模板通过识别 ZOHO ManageEngine ADSelfService Plus 面板、控制台或管理界面的页面特征，判断目标是否暴露了对应服务入口。若命中，说明该后台或运维界面对外可访问，可用于资产识别与暴露面排查。
  classification:
    cvss-metrics: CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
    cpe: cpe:2.3:a:zohocorp:manageengine_adselfservice_plus:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 2
    vendor: zohocorp
    product: manageengine_adselfservice_plus
    shodan-query:
      - http.title:"ADSelfService Plus"
      - http.title:"adselfservice plus"
      - http.title:"manageengine"
    fofa-query:
      - title="adselfservice plus"
      - title="manageengine"
    google-query:
      - intitle:"adselfservice plus"
      - intitle:"manageengine"
  tags: panel,zoho,manageengine,zohocorp,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}/authorization.do'
      - "{{BaseURL}}/servlet/GetProductVersion"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<title>ADSelfService Plus</title>'
          - '"PRODUCT_NAME":"ManageEngine ADSelfService'
        condition: or

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '"BUILD_NUMBER":"([0-9]+)"'
# digest: 4a0a00473045022100cc0d1ca010763305c9948dcc2bc73fda4d4ce7c27a3661da6a4ec65c6523f7a60220153796f869dfec5c778428e4d9e3fb41bd6acfa352580da98641092734fe7df8:922c64590222798bb761d5b6d8e72950