id: akhq-panel

info:
  name: AKHQ 面板识别（指纹识别）
  author: DhiyaneshDK
  severity: info
  description: |
    该模板通过识别 AKHQ 面板、控制台或管理界面的页面特征，判断目标是否暴露了对应服务入口。若命中，说明该后台或运维界面对外可访问，可用于资产识别与暴露面排查。
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.favicon.hash:855432563
    fofa-query: body="AKHQ_PREFIX_UI"
  tags: akhq,panel,login,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/ui/login"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "<title>AKHQ</title>"
          - "AKHQ_PREFIX_UI"
        condition: or

      - type: status
        status:
          - 200
# digest: 4a0a00473045022071df130460f9e20aa55f3c8aa3b43e60cd5bd298b22c09846b86017b4b9be71a022100d75f8b28f2ab846bb4aeab10436d41a74a7832e08513fc2db0b25a08ab8aa09a:922c64590222798bb761d5b6d8e72950