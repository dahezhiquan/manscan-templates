id: juniper-panel

info:
  name: Juniper J-Web 面板识别（暴露面识别）
  author: bhutch
  severity: info
  description: |
    该模板通过识别 Juniper J-Web 面板、控制台或管理界面的页面特征，判断目标是否暴露了对应服务入口。若命中，说明该后台或运维界面对外可访问，可用于资产识别与暴露面排查。
  reference:
    - https://www.juniper.net/documentation/us/en/software/jweb-ex/jweb-ex-application-package/topics/concept/ex-series-j-web-interface-overview.html
  classification:
    cvss-metrics: CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.title:"Juniper Web Device Manager"
  tags: panel,juniper,vpn,login,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    host-redirects: true
    max-redirects: 2
    matchers-condition: and
    matchers:
      - type: regex
        part: body
        regex:
          - '(i?)<title>(Log In - )?Juniper Web Device Manager</title>'

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        name: model
        part: body
        group: 1
        regex:
          - 'var modelphpStr = "(.*?)";'
# digest: 4b0a00483046022100ccf8d2bedf18e1872de58486e0dd50953ec2796c81b7c7c57ef28f633ed937ea022100ac1e94db852002baec8d3d4e17c1bd909dbc415ac0438b9904f4f48e8aa60bba:922c64590222798bb761d5b6d8e72950