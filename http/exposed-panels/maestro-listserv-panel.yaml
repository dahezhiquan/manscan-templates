id: maestro-listserv-panel

info:
  name: Maestro LISTSERV 识别（指纹识别）
  author: righettod
  severity: info
  description: |
    该模板通过识别 Maestro LISTSERV 面板、控制台或管理界面的页面特征，判断目标是否暴露了对应服务入口。若命中，说明该后台或运维界面对外可访问，可用于资产识别与暴露面排查。
  reference:
    - https://www.lsoft.com/
    - https://www.lsoft.com/products/maestro_11.1.asp
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.title:"LISTSERV Maestro"
  tags: panel,maestro,detect,login,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/lui/"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_all(to_lower(body), "listserv", "maestro")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)<title>[\n\r\t\s]*LISTSERV\s+Maestro[\n\r\t\s]*([0-9.-]+)[\n\r\t\s]*</title>'
          - '(?i)uniqvp=([0-9.-]+)'
# digest: 4b0a00483046022100e330c1fcf1e760c53c858675d025533336986e38e039bed04d1238edaf07a1bc022100c0fedaea7cd8c3a45c51608abc9ee521e8f68809f2627b3fba14dfd3c5580b19:922c64590222798bb761d5b6d8e72950