id: smtp-workflow

info:
  name: SMTP 安全检查工作流（工作流）
  author: pussycat0x
  description: 该工作流会在给定目标上运行所有与 SMTP 相关的 nuclei 模板。
workflows:
  - template: network/detection/smtp-detect.yaml
    subtemplates:
      - tags: smtp