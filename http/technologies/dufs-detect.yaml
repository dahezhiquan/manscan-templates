id: dufs-detect

info:
  name: DUFS 指纹识别（指纹识别）
  author: righettod
  severity: info
  description: |
    用于识别目标是否部署 DUFS 并提取相关版本或构建信息，模板会结合页面正文、特定路径或已知端点和版本或标识提取结果进行指纹匹配。
  reference:
    - https://github.com/sigoden/dufs
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.html:"Dufs"
  tags: tech,dufs,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    host-redirects: true
    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_all(to_lower(body), "__dufs_v", "allow_upload")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)__dufs_v([0-9.]+)_'
# digest: 4a0a004730450221008d2becf1e4b4baf6a244e58df2e65dd79e96d1ef37fd2d7c1c1e83920cc99c4a022036f5e9ca511db1600d4d5c5488d91af001aed52fe3c5c92ae6931fd70d17fac6:922c64590222798bb761d5b6d8e72950