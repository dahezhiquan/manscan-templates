id: vtigercrm-detect

info:
  name: Vtiger CRM 指纹识别（指纹识别）
  author: icarot
  severity: info
  description: |
    用于识别目标是否部署 Vtiger CRM 并提取相关版本或构建信息，模板会结合页面正文、特定路径或已知端点和版本或标识提取结果进行指纹匹配。
  reference:
    - https://github.com/vtiger-crm/vtigercrm
    - https://code.vtiger.com/vtiger/vtigercrm
  metadata:
    verified: true
    max-request: 1
    shodan-query: html:"Powered by vtiger CRM"
  tags: tech,vtiger,vtiger-crm,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    host-redirects: true
    max-redirects: 1
    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_all(body, "Powered by vtiger CRM", "vtiger")'
        condition: and

    extractors:
      - type: regex
        name: version
        group: 1
        part: body
        regex:
          - "Powered by vtiger CRM \\- ([0-9]{1,3}\\.[0-9]{1,3}\\.[0-9]{1,3})"
# digest: 4a0a00473045022100dda5573bd758e64db0b18c4ff1c08a9767daff74d04d5203f0ff89a49bf3316f02200be0727294ed756d178b3334b75f78dc0b139659a50858a29ba98950222735b0:922c64590222798bb761d5b6d8e72950