id: quilium-panel

info:
  name: Quilium 面板识别（指纹识别）
  author: righettod
  severity: info
  description: |
    该模板通过识别 Quilium 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  reference:
    - https://www.quilium.io/
  metadata:
    verified: true
    max-request: 2
    shodan-query: http.html:"CMS Quilium"
  tags: panel,quilium,login,detect,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}'
      - '{{BaseURL}}/en/login'

    stop-at-first-match: true

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - 'content="CMS Quilium'
          - 'Quilium</a>'
        condition: or
        case-insensitive: true

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - 'CMS Quilium ([a-f0-9.]+)'
# digest: 4b0a00483046022100b7244e07e77fef7d3f39c1dd12d3056db5286299bba4df2c8b33601b949c15d6022100e0e6bbd842837f2fdf58c580163b6e88a892f210f3a86df3bfe529067e191756:922c64590222798bb761d5b6d8e72950