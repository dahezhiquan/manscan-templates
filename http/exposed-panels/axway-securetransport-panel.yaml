id: axway-securetransport-panel

info:
  name: Axway SecureTransport 登录面板识别（暴露面识别）
  author: righettod
  severity: info
  description: |
    该模板通过识别 Axway SecureTransport 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  reference:
    - https://www.axway.com/en/products/managed-file-transfer/securetransport
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
    cpe: cpe:2.3:a:axway:securetransport:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: axway
    product: securetransport
    shodan-query:
      - http.title:"SecureTransport" || http.favicon.hash:1330269434
      - http.title:"st web client"
      - http.title:"securetransport" || http.favicon.hash:1330269434
    google-query:
      - intitle:"st web client"
      - intitle:"securetransport" || http.favicon.hash:1330269434
    fofa-query:
      - title="securetransport" || http.favicon.hash:1330269434
      - title="st web client"
  tags: panel,axway,securetransport,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}'

    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<title>Axway SecureTransport'
          - 'alt="Axway SecureTransport"'
          - 'Welcome to SecureTransport'
          - 'PrintServerInfo("SecureTransport'
        condition: or

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '"SecureTransport", "([0-9.]+)",'
# digest: 4a0a00473045022100c1e792b4915d52cd4e25f553a2198eb18801f92378476d82fc149435d55a06fc02205107474c0ccfbe7c306fe53f973fedfaa4e45cc5728998b87839b7c7f4e8cfcf:922c64590222798bb761d5b6d8e72950