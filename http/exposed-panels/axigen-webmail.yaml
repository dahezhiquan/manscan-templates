id: axigen-webmail

info:
  name: Axigen WebMail Panel 识别（暴露面识别）
  author: dhiyaneshDk,idealphase
  severity: info
  description: 该模板通过识别 Axigen WebMail 面板、控制台或管理界面的页面特征，判断目标是否暴露了对应服务入口。若命中，说明该后台或运维界面对外可访问，可用于资产识别与暴露面排查。
  reference:
    - https://www.axigen.com/
  classification:
    cwe-id: CWE-200
    cpe: cpe:2.3:a:axigen:axigen_mail_server:*:*:*:*:*:*:*:*
  metadata:
    max-request: 1
    shodan-query: http.title:"Axigen WebMail"
    product: axigen_mail_server
    vendor: axigen
  tags: axigen,panel,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: regex
        regex:
          - '(?i)(Axigen WebMail)'
          - '<title>Axigen Standard Webmail - (.*)</title>'
        condition: or

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        group: 1
        part: body
        regex:
          - '<script type="text\/javascript" src="js\/lib_login\.js\?v=(.+)"><\/script>'
# digest: 4b0a00483046022100dde822a6eacad6121ec14b8f960d6e7c9684578f1b18d06a3eefe9ec3af725a3022100e0840a04592dcd23df358a4e4a4a5942fdb9773de320ca4a54fb0437914ff058:922c64590222798bb761d5b6d8e72950