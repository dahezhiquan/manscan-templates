id: centos-workflow

info:
  name: CentOS WebPanel 安全检查工作流（工作流）
  author: daffainfo
  description: 该工作流会在给定目标上运行所有与 CentOS WebPanel 相关的 nuclei 模板。
workflows:
  - template: http/exposed-panels/cwp-webpanel.yaml
    subtemplates:
      - tags: centos
