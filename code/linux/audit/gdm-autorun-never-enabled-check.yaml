id: gdm-autorun-never-enabled-check

info:
  name: 确保 GDM 的 autorun-never 已启用（本地基线扫描）
  author: Th3l0newolf
  severity: medium
  description: |-
    此模板检查 GDM 是否配置为对可移动媒体禁用自动运行。
  remediation: |-
    启用 `autorun-never` 设置，防止内容从可移动媒体自动执行。
  reference:
    - https://www.cisecurity.org/benchmark/ubuntu_linux
  tags: cis,linux,ubuntu,gdm,autorun,media,audit
  metadata:
    verified: true

self-contained: true

code:
  - engine:
      - bash
    args:
      - "-c"
      - |
        autorun_never=$(gsettings get org.gnome.desktop.media-handling autorun-never 2>/dev/null)

        if [ "$autorun_never" = "true" ]; then
          echo "[gdm-autorun-never-enabled-check:Policy-Pass] [GDM autorun-never is enabled] [CIS_PASS] [medium]"
        else
          echo "[gdm-autorun-never-enabled-check:Policy-Fail] [GDM autorun-never is not enabled] [CIS_FAIL] [medium]"
        fi

    matchers:
      - type: word
        name: policy-pass
        words:
          - "Policy-Pass"

      - type: word
        name: policy-fail
        words:
          - "Policy-Fail"
# digest: 490a0046304402206c80c1a86743871e9f595f4dcd2a4514b546b38bc6d19526607164c2e3cae9a4022051f4b192684563ca0434b43e54043fdf0111dcd7cc59c761d5ac11d4ae9f7af0:922c64590222798bb761d5b6d8e72950
