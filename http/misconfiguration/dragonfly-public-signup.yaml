id: dragonfly-public-signup

info:
  name: DragonFly 公开注册 启用
  author: DhiyaneshDK
  severity: high
  description: |
    检测到 DragonFly 公开注册 启用。目标功能在未认证状态下可访问，可能暴露管理入口、业务数据、系统状态或可执行操作，需确认是否存在访问控制缺失。
  metadata:
    verified: true
    max-request: 1
    fofa-query: body="logo-dragonfly.png"
  tags: dragonfly,misconfig,register,signup,vuln

variables:
  username: "{{rand_base(6)}}"
  password: "{{rand_base(8)}}"
  email: "{{randstr}}@{{rand_base(5)}}.com"

http:
  - raw:
      - |
        POST /api/v1/users/signup HTTP/1.1
        Host: {{Hostname}}
        Content-Type: application/json;charset=UTF-8

        {"name":"{{username}}","password":"{{password}}","email":"{{email}}","passwordT":"{{password}}"}

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '"id":'
          - '"created_at":'
          - '"updated_at":'
          - '"state":'
        condition: and

      - type: word
        part: content_type
        words:
          - "application/json"

      - type: status
        status:
          - 200

    extractors:
      - type: dsl
        dsl:
          - '"username: "+ username'
          - '"password: "+ password'
# digest: 4a0a004730450221008ac1d151786ebb35f4aeddc59e720624d377b6baf46a9beba057df783be6aae702206d387f1f267a082df0a3cc7e28028a4e5100c85fb3032e2b3624503cad7774f7:922c64590222798bb761d5b6d8e72950