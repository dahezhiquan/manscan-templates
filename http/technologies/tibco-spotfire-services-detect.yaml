id: tibco-spotfire-services-detect

info:
  name: TIBCO Spotfire Statistics 服务指纹识别（指纹识别）
  author: righettod
  severity: info
  description: |
    用于识别目标是否部署 TIBCO Spotfire Statistics 服务 并提取相关版本或构建信息，模板会结合页面正文、特定路径或已知端点和版本或标识提取结果进行指纹匹配。
  reference:
    - https://docs.tibco.com/products/tibco-spotfire-statistics-services
  classification:
    cpe: cpe:2.3:a:tibco:spotfire_statistics_services:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 4
    vendor: tibco
    product: spotfire_statistics_services
    shodan-query: "TIBCO Spotfire Server"
  tags: tech,tibco,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/SplusServer/"
      - "{{BaseURL}}/RServer/"
      - "{{BaseURL}}/TERR/"
      - "{{BaseURL}}"

    stop-at-first-match: true
    matchers:
      - type: dsl
        dsl:
          - 'contains_any(to_lower(body), "tibco spotfire statistics services", "spotfire statistics services")'
          - 'contains(header, "TIBCO Spotfire Server")'
        condition: or

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - 'Version\s+([0-9.\s\(\)V]+)'
# digest: 4b0a00483046022100d90e2df0c1c9e06d46eb81589930b87dfca5d08c21dcb5482eff8efc6ad4e18b022100ddb21583ea4ed8ec1c160fa8014ba3c7a79c4d75ef3e3af550bfd689fce3ecb4:922c64590222798bb761d5b6d8e72950