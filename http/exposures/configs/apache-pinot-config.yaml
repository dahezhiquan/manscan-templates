id: apache-pinot-config

info:
  name: Apache Pinot 暴露（暴露面识别）
  author: icarot
  severity: medium
  description: |
    检测到对外可访问的 Apache Pinot。这类配置资源可能包含部署细节、访问凭据、内部地址、组件版本或调试开关，公开暴露后会显著增加被侦察和后续利用的风险。
  classification:
    cpe: cpe:2.3:a:apache:pinot:*:*:*:*:*:*:*:*
  metadata:
    max-request: 1
    verified: true
    vendor: apache
    product: pinot
    shodan-query: title:"Apache Pinot"
  tags: config,exposure,pinot,apache,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}/appconfigs"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '"systemConfig"'
          - '"pinotConfig"'
          - '"jvmConfig"'
        condition: and

      - type: word
        part: header
        words:
          - 'application/json'

      - type: status
        status:
          - 200
# digest: 4a0a00473045022100a2ac31e376a5a089cdf49c09339edc360e5b5610444963b57f627211dce3db8d02207a4112dcd332e16cb8d7734398c222d6f4a6652791a4796b0de98db61aab02ac:922c64590222798bb761d5b6d8e72950