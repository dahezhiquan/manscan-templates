id: netdata-dashboard-detect

info:
  name: Netdata Dashboard 面板识别（指纹识别）
  author: pussycat0x
  severity: info
  description: 该模板通过识别 Netdata Dashboard 仪表盘或后台界面的页面特征，判断目标是否对外暴露了相关控制台。若命中，说明该服务面向外部可访问，可用于资产识别与访问控制核查。
  classification:
    cvss-metrics: CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
    cpe: cpe:2.3:a:netdata:netdata:*:*:*:*:*:*:*:*
  metadata:
    max-request: 1
    vendor: netdata
    product: netdata
    shodan-query:
      - 'Server: NetData Embedded HTTP Server'
      - http.title:"netdata dashboard"
      - "server: netdata embedded http server"
    fofa-query: title="netdata dashboard"
    google-query: intitle:"netdata dashboard"
  tags: netdata,panel,tech,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<title>netdata dashboard</title>'

      - type: status
        status:
          - 200
# digest: 490a004630440220224bcae0765eed70b41cae520bb521dd93f620c420314d8b32d4c9a7916e9c1602201938d4f931def4f7ec03ed8f8ff7063e24483512855decb50968ec750ba376f0:922c64590222798bb761d5b6d8e72950