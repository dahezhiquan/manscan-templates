id: opache-control-panel

info:
  name: Opache 控制面板 未授权 访问
  author: pussycat0x
  severity: medium
  description: |
    检测到 Opache 控制面板 未授权 访问。目标功能在未认证状态下可访问，可能暴露管理入口、业务数据、系统状态或可执行操作，需确认是否存在访问控制缺失。
  metadata:
    verified: true
    max-request: 2
    shodan-query: http.title:"Opcache Control Panel"
  tags: misconfig,opache,exposure,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/ocp.php"

    stop-at-first-match: true
    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_all(body, "Opcache Control Panel", "Reset", "Files")'
        condition: and
# digest: 4b0a0048304602210097003417265e9153a507d3f27a2019ef1930fae0a5785957b98ac9894f0657ff022100b859802c9187f3638b82f0c620577714b2ff4bfb2bbed5676bc68838155e87f5:922c64590222798bb761d5b6d8e72950