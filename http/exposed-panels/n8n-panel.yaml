id: n8n-panel

info:
  name: n8n 面板识别（暴露面识别）
  author: userdehghani,rxerium
  severity: info
  description: |
    该模板通过识别 n8n 面板、控制台或管理界面的页面特征，判断目标是否暴露了对应服务入口。若命中，说明该后台或运维界面对外可访问，可用于资产识别与暴露面排查。
  reference:
    - https://n8n.io/
  classification:
    cpe: cpe:2.3:a:n8n:n8n:*:*:*:*:node.js:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: n8n
    product: n8n
    shodan-query: http.favicon.hash:-831756631
  tags: panel,n8n,login,detect,discovery,ai

http:
  - method: GET
    path:
      - "{{BaseURL}}/signin"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<title>n8n.io'
        case-insensitive: true

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        name: base64_content
        group: 1
        regex:
          - '<meta name="n8n:config:sentry" content="([A-Za-z0-9+/=]+)"'
        internal: true

      - type: dsl
        name: version
        dsl:
          - 'replace_regex(base64_decode(base64_content), ".*n8n@([0-9]+\\.[0-9]+\\.[0-9]+).*", "$1")'
        internal: true

      - type: dsl
        dsl:
          - '"n8n Version: " + version'
# digest: 4a0a004730450220663c9c73fc566b0fc482592237b4dd9cfee47c465cf35ded5a5778bbcf86dcc5022100e7d10ad664649b71f4af0d8153a68e3c0997e560d8f1a0eeedfdf8c5f32f663c:922c64590222798bb761d5b6d8e72950