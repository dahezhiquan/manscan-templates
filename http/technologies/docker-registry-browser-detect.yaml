id: docker-registry-browser-detect

info:
  name: Docker Registry Browser 指纹识别（指纹识别）
  author: pussycat0x
  severity: info
  description: |
    用于识别目标是否使用 Docker Registry Browser，模板会结合页面标题、页面正文、状态码和特定路径或已知端点进行技术指纹匹配。
  reference:
    - https://github.com/klausmeyer/docker-registry-browser
  metadata:
    verified: true
    max-request: 1
    shodan-query: html:"Docker Registry Browser"
  tags: docker,registry,browser,ui,tech,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "<title>Docker Registry Browser</title>"

      - type: status
        status:
          - 200
# digest: 4a0a004730450221009250788213e8b0e7998c6cbb7f990a5134dcd1c5e2e6f0652b07b29fc36a1d1c02204a5c811d2e2b3ac66892a8b5c3e467413e24ba60c3442ae9983acc9b2005b466:922c64590222798bb761d5b6d8e72950