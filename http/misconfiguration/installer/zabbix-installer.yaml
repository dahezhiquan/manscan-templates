id: zabbix-installer

info:
  name: Zabbix 安装 暴露
  author: DhiyaneshDK
  severity: high
  description: |
    检测到 Zabbix 安装 暴露。该页面属于安装、初始化或配置向导流程，若对外暴露，攻击者可能完成初始化、创建管理员账户或重新配置目标系统。
  metadata:
    verified: true
    max-request: 1
    shodan-query:
      - http.favicon.hash:892542951
      - http.title:"zabbix-server"
      - cpe:"cpe:2.3:a:zabbix:zabbix"
    product: zabbix
    vendor: zabbix
    fofa-query:
      - icon_hash=892542951
      - app="zabbix-监控系统" && body="saml"
      - title="zabbix-server"
    google-query: intitle:"zabbix-server"
  tags: misconfig,zabbix,install,exposure,vuln

http:
  - method: GET
    path:
      - '{{BaseURL}}/setup.php'

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<title>Installation</title>'
          - 'zabbix'
        condition: and

      - type: word
        part: header
        words:
          - "text/html"

      - type: status
        status:
          - 200
# digest: 4a0a00473045022100fb9e7c25330c1352576a933c8fd01856765f4a2d73292977821b20ff9d349be502202f4a415b7fc7664a09495100cee6aebf5cb50bce3bb9fa6a373268bccdae3f63:922c64590222798bb761d5b6d8e72950