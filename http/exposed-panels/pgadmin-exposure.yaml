id: pgadmin-exposure

info:
  name: PostgreSQL pgAdmin Dashboard 面板识别（暴露面识别）
  author: princechaddha
  severity: info
  description: 该模板通过识别 PostgreSQL pg 仪表盘或后台界面的页面特征，判断目标是否对外暴露了相关控制台。若命中，说明该服务面向外部可访问，可用于资产识别与访问控制核查。
  classification:
    cvss-metrics: CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
  metadata:
    max-request: 1
  tags: unauth,pgadmin,postgres,panel,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/browser/"

    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200

      - type: word
        words:
          - "PGADMIN_LANGUAGE="
        part: header

      - type: word
        words:
          - "<title>pgAdmin"
        part: body
# digest: 490a004630440220619af821811d05aa60fabf8ccf95035b68955be209246dfbb00a56ca0a9772540220562d71740c0b1508c161fb027def89a5bc44d82df22106309679dab08e478331:922c64590222798bb761d5b6d8e72950