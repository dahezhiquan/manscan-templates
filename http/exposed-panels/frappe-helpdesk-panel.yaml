id: frappe-helpdesk-panel

info:
  name: Frappe Helpdesk 登录面板识别（暴露面识别）
  author: righettod
  severity: info
  description: |
    该模板通过识别 Frappe Helpdesk 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  reference:
    - https://frappe.io/helpdesk
    - https://github.com/frappe/helpdesk
  metadata:
    max-request: 1
    verified: true
    shodan-query: http.html:"window.frappe_version"
  tags: panel,frappe,login,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/helpdesk/login"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_all(to_lower(body), "/helpdesk/", "frappe")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)helpdesk_version\s*=\s*"([0-9.]+)"'
# digest: 4a0a00473045022012eac45d7bbc4c7e6e0c6f0d19c5aabe0842d9a2569e8512867b0a4b7cae094d022100b5d1773bb4a2ee02ce4a719ea7ab0020ca26990ad5197087263f81df678b9353:922c64590222798bb761d5b6d8e72950