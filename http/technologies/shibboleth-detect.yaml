id: shibboleth-detect

info:
  name: Shibboleth SSO 指纹识别（指纹识别）
  author: rxerium
  severity: info
  description: |
    用于识别目标是否使用 Shibboleth SSO，模板会结合页面标题、页面正文、状态码和特定路径或已知端点进行技术指纹匹配。
  reference:
    - https://www.shibboleth.net/
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.title:"Shibboleth IdP"
  tags: shibboleth,sso,detect,tech,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/shibboleth-idp"

    stop-at-first-match: true

    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<title>Shibboleth IdP</title>'
        case-insensitive: true

      - type: status
        status:
          - 200
# digest: 4b0a00483046022100b767eba2d8743faad09dd7f6834947ea48e6145fa288d6ab6e5dfe1ec8d63017022100ce5e644d8da56dd00080e93298c9ced1ddf6b4931cd53df7847c9fce05d12c46:922c64590222798bb761d5b6d8e72950