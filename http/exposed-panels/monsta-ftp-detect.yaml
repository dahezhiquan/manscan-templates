id: monsta-ftp-detect

info:
  name: Monsta FTP 识别（暴露面识别）
  author: rxerium
  severity: info
  description: |
    该模板用于识别 Monsta FTP 相关页面或服务入口是否对外暴露。若命中，说明目标可被识别为对应产品或组件，可用于资产发现与攻击面管理。
  reference:
    - https://www.monstaftp.com/
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.title:"Monsta FTP"
  tags: tech,monsta,ftp,detect

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        words:
          - 'ng-app="MonstaFTP"'
          - '<title>Monsta FTP'
        condition: and

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        name: version
        group: 1
        regex:
          - 'monsta-min-([0-9.]+)\.js'
# digest: 4b0a00483046022100ef92ad74f3890bd1340284e4001e6814246f08be5b65262a4568572d8182742f022100de35c0babdf1c8c3634e0ce8f34bc9b30448e169f786636f342b636ecf3d95a3:922c64590222798bb761d5b6d8e72950