id: smb-default-creds

info:
  name: SMB 默认凭据爆破（服务漏洞）
  author: pussycat0x
  severity: high
  description: |
    该模板通过 SMB 协议尝试常见默认账号口令，识别文件共享服务是否存在弱凭据风险。
  reference:
    - https://nmap.org/nsedoc/scripts/smb-brute.html
  metadata:
    verified: true
    max-request: 9
    shodan-query: "port:445"
    product: dionaea
    vendor: dionaea
  tags: js,network,smb,enum,default,bruteforce,discovery

javascript:
  - pre-condition: |
      isPortOpen(Host,Port);
    code: |
      var m = require("nuclei/smb");
      var c = new m.SMBClient();
      var response = c.ListShares(Host, Port, User, Pass);
      response;

    args:
      Host: "{{Host}}"
      Port: "445"
      User: "{{usernames}}"
      Pass: "{{passwords}}"

    attack: clusterbomb
    payloads:
      usernames:
        - 'admin'
        - 'administrator'
        - 'guest'
      passwords:
        - 'admin'
        - 'password'
        - 'guest'

    stop-at-first-match: true
    matchers:
      - type: dsl
        dsl:
          - 'response != "[]"'
          - 'success == true'
        condition: and
# digest: 4a0a004730450220423768d61d69acfdd8e9d931afa253611b039ea8b515a2a9acc62a40ff768ccc022100e6a9d9a03da49e2e173ea503935fc34bed9a88d109334ebdc988d6931fe629b3:922c64590222798bb761d5b6d8e72950