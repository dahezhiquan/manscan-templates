id: cloudlog-panel

info:
  name: Cloudlog 面板识别（暴露面识别）
  author: s4e-io
  severity: info
  description: |
    该模板通过识别 Cloudlog 面板、控制台或管理界面的页面特征，判断目标是否暴露了对应服务入口。若命中，说明该后台或运维界面对外可访问，可用于资产识别与暴露面排查。
  reference:
    - https://github.com/magicbug/Cloudlog
    - https://lab.uberspace.de/guide_cloudlog/
  metadata:
    verified: true
    max-request: 1
    fofa-query: title="Login - Cloudlog"
  tags: panel,login,cloudlog,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/index.php/user/login"

    stop-at-first-match: true
    host-redirects: true
    max-redirects: 2
    matchers:
      - type: dsl
        dsl:
          - 'contains_any(body, "<title>Login - Cloudlog", "Login to Cloudlog", "/CloudLog_logo.png")'
          - 'status_code == 200'
        condition: and
# digest: 490a004630440220122ff35ab3a0ac6ce4fe0022d6bdb1178aed634501faa3cc997750859bdb32a8022030d9f333af4c667114e3b54d9a8f2708908135ecc1102f5e5793f39b2d1c6cb8:922c64590222798bb761d5b6d8e72950