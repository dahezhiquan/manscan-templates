id: mguard-security-appliance-panel

info:
  name: mGuard 安全设备 - 面板（暴露面识别）
  author: righettod
  severity: info
  description: |-
    检测到 mGuard 安全设备——一种工业防火墙/安全路由器，用于保护机器、PLC、HMI 及工业网络。
  reference:
    - https://www.phoenixcontact.com/
  metadata:
    max-request: 1
    shodan-query: http.title:"mGuard"
  tags: panel,phoenixcontact,login,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: or
    matchers:
      - type: dsl
        dsl:
          - contains_any(to_lower(body), '<title>mguard</title>', '<span id="hostname">mguard</span>')
          - status_code == 200
        condition: and

      - type: dsl
        dsl:
          - contains(header, 'mGuard')
          - status_code == 200
        condition: and
# digest: 4a0a00473045022068102adb40397bb01208a399bc68e9166bee2ee2cbcaa5f838076d0e871826fe022100a947ca4f4010b1fd5d09f0aae76b5759956b11c74ef5c35dd4485441f48c6e8c:922c64590222798bb761d5b6d8e72950
