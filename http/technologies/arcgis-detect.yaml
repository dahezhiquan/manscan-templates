id: arcgis-detect

info:
  name: ArcGIS 指纹识别（指纹识别）
  author: righettod
  severity: info
  description: |
    用于识别目标是否部署 ArcGIS 并提取相关版本或构建信息，模板会结合页面标题、页面正文、特定路径或已知端点和版本或标识提取结果进行指纹匹配。
  reference:
    - https://enterprise.arcgis.com/en/
  metadata:
    verified: true
    max-request: 5
    shodan-query: http.title:"ArcGIS"
  tags: tech,arcgis,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/portal/sharing/rest"
      - "{{BaseURL}}/portal/portalhelp/en/rest/"
      - "{{BaseURL}}/arcgis/rest/services"
      - "{{BaseURL}}/server/rest/services"
      - "{{BaseURL}}/arcgis/"

    stop-at-first-match: true
    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_any(to_lower(body), "<title>arcgis portal directory", "search arcgis enterprise help", "<title>arcgis</title>", "arcgis rest services directory", "<title>arcgis hub</title>")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)&copy;\s+([0-9]+)\s+Esri\.'
          - '<b>Current\s+Version:\s+<\/b>([0-9.]+)'
# digest: 4b0a00483046022100dfacd916bcabe837b687354904aaf1637b8febc2e0c8e6e512e7ef8148cb0698022100af59ee669ee2030369a3ddf5b81c61b92e6d4964bfd5904a14924ede30b87898:922c64590222798bb761d5b6d8e72950