id: gradle-develocity-panel

info:
  name: Gradle Develocity Build Cache Node 登录面板识别（指纹识别）
  author: righettod
  severity: info
  description: 该模板通过识别 Gradle Develocity Build Cache Node 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  reference:
    - https://gradle.com/gradle-enterprise-solutions/
  classification:
    cpe: cpe:2.3:a:gradle:build_cache_node:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: gradle
    product: build_cache_node
    shodan-query: http.html:"Develocity Build Cache Node"
  tags: panel,gradle,detect,login,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}'

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200 || status_code == 401'
          - 'contains_any(to_lower(body), "develocity build cache node", "develocity", "com.gradle.error.fallback")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)"applicationVersion":"([0-9.]+)"'
# digest: 490a0046304402206d67e6bb860301bb7a8858daf2fee8bcbafe83cb558d35e7f0eb3fdf542755200220473ed4f4055ef4878460d440d6d9d0bb52f1940765948a0f6dea4bd04fe0faa3:922c64590222798bb761d5b6d8e72950