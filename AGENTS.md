# AGENTS.md

本文件定义在本仓库中处理漏洞模板时的强制执行规则。后续所有 AI 在处理模板前，都必须先遵守这里的要求。

## 通用要求

- 默认使用中文沟通。
- 修改前先理解目标模板的用途、验证方式和影响范围，避免机械改写。
- 除非用户明确要求，否则不要批量改动无关文件。
- 修改模板时，优先保持原有结构、缩进、字段顺序和语义风格稳定。
- 模版文件后缀只能使用 `.yaml` 或 `.json`，不得新建、输出或要求使用其他后缀格式。

## 新建模板要求

当用户要求“写新模版”“新增模版”“补一个模版”时，必须严格遵守 [CODE_STYLE.md](CODE_STYLE.md) 中的全部规范，不得自行简化、跳过或替换其中的要求。

执行时至少满足以下几点：

- 严格按照 `CODE_STYLE.md` 约定处理目录、`id`、文件名、`info`、`reference`、`metadata`、`tags`、协议块顺序与匹配逻辑。
- 优先控制误报，先证明漏洞，再设计匹配器。
- 模版内容必须可读、可维护、可复核，不能只追求“能跑”。
- 新增模版前，必须先根据 `POC` 内容判断仓库中是否已经存在相同或高度相似的模版；优先复用、合并或改造已有模版，避免重复新增。
- 如果 `CODE_STYLE.md` 与个人习惯冲突，以 `CODE_STYLE.md` 为准。

## 新增或修改模板的标签规范

当新增模板，或修改已有模板的 `info.tags` 时，必须额外执行以下检查与维护动作。

重要：标签检查默认仅做不规范提示，不得因为检查结果直接修改模板标签。只有用户明确要求修复标签，或当前任务本身包含新增/修改模板标签时，才允许按要求调整 `info.tags`。

### 标签质量检查

- 必须检查 `tags` 中是否存在特别怪的标签，包括但不限于非产品名称所需的额外标点符号、空标签、连续逗号、前后空格、大小写混用、字符错乱、明显错别字或拼写错误。
- 如果标签中包含产品官方名称必需的特殊字符，应结合仓库既有标签风格判断是否保留；若没有必要，检查结果中应提示可改成小写、无空格、英文逗号分隔的 slug 风格。
- 必须检查仓库中是否已经存在相同含义的标签；如果已有同义或更规范的标签，检查结果中必须提示复用已有标签，避免新增命名冗余的近义标签。
- 必须严格遵守 [CODE_STYLE.md](CODE_STYLE.md) 中 `tags` 的要求，包括使用英文小写短词、英文逗号分隔、不包含空格，并尽量覆盖“厂商 + 产品 + 漏洞类型 + 类别”。
- 如果是指纹类模板，即模板 `tags` 中包含 `tech`、`detect`、`favicon` 任意一个，则该指纹模板对应产品的产品标签必须能在模板 `id` 和 `info.name` 中找到对应产品语义；如果产品标签与 `id` 或 `info.name` 不一致，必须作为不规范项提示。
- CVE 模板必须检查 `cve` 与 `cveYYYY` 年份标签是否与模板编号一致；如果发现缺失年份、年份写错或多个冲突年份标签，必须作为不规范项提示。

### 标签资料同步

- 新增模板或修改模板 `tags` 后，必须同步更新 [自动模板映射标签清单.md](自动模板映射标签清单.md)。
- 新增模板或修改模板 `tags` 后，必须同步更新 [模板标签统计.md](模板标签统计.md)。
- 更新上述两个文档时，应确保新增、删除、重命名或合并后的标签统计结果与模板实际 `tags` 保持一致。

## 汉化模板要求

当用户要求“汉化模版内容”“把某目录模板汉化”“翻译模板”为中文时，按以下规则执行。

### 范围要求

- 必须处理用户指定目录里的所有模板，不能只挑部分文件。
- 如果用户指定的是一个目录，则默认递归处理该目录下所有模板文件。
- 如果目录中存在多个模板，不能只修改一个示例后停止。

### 修改边界

- 只允许修改模板中的 `info` 块内容。
- 除 `info` 块外，禁止修改任何其他内容。
- 特别强调：不要改动请求、匹配器、变量、payload、flow、签名、注释、顶层结构或任意协议块。

### `info` 字段处理规则

#### `name`

- 模板的 `name` 必须改成如下格式：

  `汉化版的漏洞名称 + 用户指定的后缀`

- 这里的“用户指定的后缀”必须原样拼接到中文漏洞名称后面。
- 名称要自然、专业、准确，不能只是生硬直译。
- 如果模板中存在 `variables` 里的 `name` 字段，不要改动；这里只修改 `info.name`。

#### 目录后缀规则

- 汉化模板时，`info.name` 还必须按所在目录追加固定后缀。
- 如果模板 `tags` 中包含 `tech`、`detect`、`favicon` 任意一个，则 `info.name` 后缀直接使用 `（指纹识别）`，不再考虑所在目录后缀。
- 同一模板只能使用一个后缀，禁止重复拼接相同后缀，也禁止叠加多个目录后缀。
- 若原始 `name` 已经包含对应后缀语义，仍只保留一次，不要重复写。

固定对应关系如下：

- `cloud/` -> `（云安全）`
- `code/` -> `（本地基线扫描）`
- `dast/` -> `（DAST）`
- `dns/` -> `（dns）`
- `file/` -> `（本地文件安全扫描）`
- `headless/` -> `（headless）`
- `javascript/` -> `（服务漏洞）`
- `network/` -> `（服务漏洞）`
- `ssl/` -> `（证书安全）`
- `workflows/` -> `（工作流）`
- `http/cnvd/` -> `（CNVD-编号）`，例如 `（CNVD-2018-13393）`
- `http/credential-stuffing/` -> `（撞库）`
- `http/cves/` -> `（CVE-编号）`，例如 `（CVE-2005-3128）`
- `http/default-logins/` -> `（弱口令）`
- `http/exposed-panels/` -> `（暴露面识别）`
- `http/exposures/` -> `（暴露面识别）`
- `http/global-matchers/` -> `（全局模版）`
- `http/honeypot/` -> `（蜜罐识别）`
- `http/iot/` -> `（IOT）`
- `http/osint/phishing/` -> `（钓鱼网站识别）`
- `http/osint/user-enumeration/` -> `（OSINT）`
- `http/takeovers/` -> `（子域名接管）`
- `http/technologies/` -> `（指纹识别）`
- `http/token-spray/` -> `（token喷洒）`

#### `description` / `impact` / `remediation`

- 需要将 `description`、`impact`、`remediation` 改写为高质量中文。
- 如果某个字段不存在，不要新增该字段。
- 不能只做字面翻译，必须结合该模板的漏洞类型、受影响产品、验证方式、命中证据和利用后果进行润色，使内容更贴合当前模板。
- 结果不要泛泛而谈，避免空话，例如“可能导致安全风险”“建议及时修复”这类缺少指向性的描述应尽量改得更具体。
- 重点提示：汉化完成后，必须检查 `info` 块中是否残留原英文说明，避免出现中英混杂或翻译后忘记删除原文的情况；如确需保留英文专有名词，应确保其为产品名、组件名、漏洞编号或必要术语，而不是整句原文残留。

### 参考资料使用要求

- 对于每个模板，必须认真对待，不能批量套用同一段说明。
- 需要优先查看模板 `reference` 中的所有链接，尽量提取漏洞背景、影响条件、利用方式、修复建议等信息。
- 访问参考链接时，必须使用 `Vuln Page Extractor` skill 尝试提取漏洞内容。
- 如果 `reference` 中有多个链接，应尽量都访问，而不是只看第一个。
- 如果参考资料与模板检测逻辑存在差异，应以模板实际验证方式为基础，对中文描述做针对性修正。

### 无法访问参考链接时的降级策略

- 如果无法访问链接内容、页面无有效正文、或提取结果不足以支撑高质量改写，则先基于原模板文本翻译成通顺、专业的中文。
- 在仅能翻译原文的情况下，仍应结合模板中的验证方式、命中条件、返回特征或利用路径，对 `description`、`impact`、`remediation` 做适度润色，让内容更贴近该模板本身。

### 列表格式要求

- `impact` 和 `remediation` 如果包含多条内容，必须使用 Markdown 列表格式，每一条以前缀 `- ` 展示。
- 不要使用编号列表替代。
- 不要把多条危害或修复建议压缩进一个长句中。

## 质量要求

- 不要为了追求速度牺牲准确性。
- 每个模板的中文内容都应与该模板自身匹配，避免跨模板复用不相干的说明。
- 中文应自然、专业、简洁，适合直接作为漏洞模板元信息使用。
- 如果原模板描述明显失真、过时或过于空泛，可在不超出 `info` 范围的前提下进行纠正和增强。

## 执行前自检

提交前至少检查以下内容：

- 是否严格遵守 `CODE_STYLE.md`（新建模板时）。
- 是否覆盖了用户指定目录中的全部模板（汉化时）。
- 是否只修改了 `info` 块。
- 是否没有误改 `variables` 中的 `name`。
- 是否将 `info.name` 改成“汉化版漏洞名称 + 用户指定后缀”。
- 是否尽可能访问并利用了 `reference` 中的所有链接。
- 是否在无法访问链接时做了合理翻译与基于验证逻辑的润色。
- 是否将多条 `impact` / `remediation` 改成了 `- ` 列表格式。
- 是否检查并清理了汉化后遗留在 `info` 块中的原英文内容，避免出现中英混杂或翻译后忘记删除原文的情况。
- 若新增模板或修改了模板 `tags`，是否完成标签异常检查、提示仓库已有同义标签，并同步更新 `自动模板映射标签清单.md` 与 `模板标签统计.md`。
