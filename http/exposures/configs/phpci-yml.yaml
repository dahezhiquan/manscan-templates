id: phpci-yml

info:
  name: PHPCI 配置暴露 "phpci.YML" 暴露（暴露面识别）
  author: DhiyaneshDk
  severity: info
  description: |
    检测到对外可访问的 PHPCI 配置暴露 "phpci.YML"。这类配置资源可能包含部署细节、访问凭据、内部地址、组件版本或调试开关，公开暴露后会显著增加被侦察和后续利用的风险。
  reference:
    - https://github.com/dancryer/PHPCI/blob/master/.phpci.yml
  metadata:
    verified: true
    max-request: 1
    fofa-query: body="phpci.yml"
  tags: phpci,misconfig,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}/phpci.yml"
      - "{{BaseURL}}/ci/phpci.yml"

    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - 'build_settings:'
          - 'setup:'
          - 'php_unit:'
        condition: and

      - type: word
        part: content_type
        words:
          - 'application/octet-stream'
          - "application/x-yaml"
          - "text/yaml"
          - "text/plain"
        condition: or

      - type: status
        status:
          - 200
# digest: 4a0a00473045022100a3a11da8e5b4efea4b9df458b6fb02b9f1a434102e2144b518693b242ed34869022044eb3777907cd2cfd6059c27c2678387ca9c95345b7dd6e7f4f54cebbf473dd5:922c64590222798bb761d5b6d8e72950