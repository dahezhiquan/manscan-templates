id: netgear-version-detect

info:
  name: NETGEAR Router 面板识别（暴露面识别）
  author: dwisiswant0
  severity: info
  description: 该模板通过识别 NETGEAR Router 面板、控制台或管理界面的页面特征，判断目标是否暴露了对应服务入口。若命中，说明该后台或运维界面对外可访问，可用于资产识别与暴露面排查。
  classification:
    cvss-metrics: CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
  metadata:
    max-request: 1
  tags: panel,router,netgear,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/currentsetting.htm"

    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200

      - type: regex
        part: body
        regex:
          - "^((((Re(adyShareSupportedLevel|gionTag)|InternetConnectionStatus|SOAPVersion|Firmware|Model)|Region)|LoginMethod)|ParentalControlSupported)="

    extractors:
      - type: regex
        part: body
        regex:
          - "Model=([^\\s]+)"
          - "Firmware=([^\\s]+)"
# digest: 4b0a00483046022100ed7e6c583a8dc59dbaa0c7d23c0ddca969ca065f8a05bedfe3f76a258547fb0e022100915d5f545e0587ae33a8d75bbd59cfa7b43b200f79205c74a99e9410d9cea274:922c64590222798bb761d5b6d8e72950