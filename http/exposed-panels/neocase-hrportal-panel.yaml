id: neocase-hrportal-panel

info:
  name: Neocase HR Portal 登录面板识别（暴露面识别）
  author: righettod
  severity: info
  description: |
    该模板通过识别 Neocase HR Portal 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  reference:
    - https://www.neocasesoftware.com/neocase-hr-solution/
    - https://www.neocasesoftware.com/self-service-portal-module/
  metadata:
    verified: true
    max-request: 1
  tags: panel,neocase,login,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains(to_lower(body), "neocase") && contains(to_lower(body), "hr portal") && contains(to_lower(body), "login")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - 'name="version"\s+content="([0-9\.]+)"'
# digest: 4a0a00473045022100aad4a0382ff4f4e46044841d86ef05137b24784abf4262a16a34e28c86ed62d9022056e1644191dcd92f1183b66a0dd4725b611dca48c67cf35478f8860f98665ecb:922c64590222798bb761d5b6d8e72950