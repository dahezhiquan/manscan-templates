id: apache-activemq-artemis-detect

info:
  name: Apache ActiveMQ Artemis 指纹识别（指纹识别）
  author: icarot
  severity: info
  description: |
    用于识别目标是否部署 Apache ActiveMQ Artemis 并提取相关版本或构建信息，模板会结合页面标题、页面正文、特定路径或已知端点和版本或标识提取结果进行指纹匹配。
  reference:
    - https://github.com/apache/activemq-artemis/
  metadata:
    max-request: 1
    vendor: apache
    product: activemq
    shodan-query: title:"ActiveMQ Artemis Console"
  tags: tech,activemq,artemis,apache,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/console/hawtconfig.json"

    matchers-condition: and
    matchers:
      - type: word
        words:
          - 'img/activemq.png'
          - 'ActiveMQ Artemis'
          - 'Apache Artemis'
        condition: or

    extractors:
      - type: regex
        part: body
        name: version
        group: 1
        regex:
          - '"productInfo":.*"value":\s*"(.*)"'
# digest: 4a0a00473045022100cb76f333c7e12ef965e31b503bb8b074c38e9773e31ed01a7f71db6818ace0f102201e936acd73df2e1fb8377b4ba9b22dd647c141f1d1f9459e98b5aafe042fd974:922c64590222798bb761d5b6d8e72950