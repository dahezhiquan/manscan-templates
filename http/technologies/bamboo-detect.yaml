id: bamboo-detect

info:
  name: Bamboo 指纹识别（指纹识别）
  author: bhutch
  severity: info
  description: |
    用于识别目标是否部署 Bamboo 并提取相关版本或构建信息，模板会结合 favicon 哈希、页面标题、特定路径或已知端点和版本或标识提取结果进行指纹匹配。
  classification:
    cpe: cpe:2.3:a:atlassian:bamboo:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: atlassian
    product: bamboo
    shodan-query: http.favicon.hash:-1379982221
    category: devops
  tags: tech,bamboo,atlassian,detect,cicd,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    redirects: true
    matchers:
      - type: dsl
        dsl:
          - contains(to_lower(body), "<title>log into atlassian - atlassian bamboo</title>")
          - contains(to_lower(body), "meta name=\"application-name\" content=\"bamboo\" />")

    extractors:
      - type: regex
        name: version
        group: 1
        regex:
          - '(?i)atlassian bamboo</a> version (.*) -'
# digest: 4b0a00483046022100b60c556626bcdcc9ece11b4cce33b5ee8b9e26787fc19d8006526c261a35ac0402210088c01231bef5a32ed790b60585fb1fea34a8e4ae6e7d4bf0c8a7d0950b8abb32:922c64590222798bb761d5b6d8e72950