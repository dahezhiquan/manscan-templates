id: dmarc-detect

info:
  name: DMARC 记录识别（dns）
  author: juliosmelo
  severity: info
  description: |
    该模板检测 DNS 中的 DMARC 配置。DMARC 基于 SPF 和 DKIM 结果定义邮件认证失败时的处理策略，可帮助降低域名被冒用发送邮件的风险。
  reference:
    - https://dmarc.org/
    - https://dmarc.org/wiki/FAQ#Why_is_DMARC_important.3F
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cvss-score: 0
    cwe-id: CWE-200
  metadata:
    max-request: 1
  tags: dns,dmarc,discovery

dns:
  - name: "_dmarc.{{FQDN}}"
    type: TXT
    matchers:
      - type: regex
        part: answer
        regex:
          - "IN\tTXT\\t(.+DMARC.+)$"

    extractors:
      - type: regex
        group: 1
        regex:
          - "IN\tTXT\t(.+DMARC.+)"
# digest: 490a0046304402203b8f5eb06ccf503031d6e5446e57fee1b9479475424bb02a01aa0f8a6aa49d28022037ebb60579921a56b68a4c3a35ad70b2e35ee0abee236b7c0d9d3882f5f2e5d9:922c64590222798bb761d5b6d8e72950