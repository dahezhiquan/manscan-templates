id: gcloud-uniform-bucket-level-access-not-enabled

info:
  name: Enable Uniform Bucket-Level 访问 的Cloud Storage Buckets（云安全）
  author: princechaddha
  severity: medium
  description: 该规则用于检查 GCP 存储账户 相关身份、权限或口令安全策略是否符合最小权限和强认证要求，避免因过度授权或弱认证导致资源被滥用。
  impact: |
    - 过度授权、弱口令策略或缺少 MFA 会显著提升账户被滥用的风险。
    - 一旦身份被攻破，攻击者可能借助合法权限直接访问或控制云资源。
  remediation: |
    - 落实最小权限原则，收敛高危权限、通配授权和不必要的管理员能力。
    - 对关键账户和控制台访问强制启用 MFA，并完善口令复杂度、轮换与重用限制策略。
  reference:
    - https://cloud.google.com/storage/docs/uniform-bucket-level-access
  tags: cloud,devops,gcp,gcloud,google-cloud-storage,security,access-control,gcp-cloud-config

flow: |
  code(1)
  for(let projectId of iterate(template.projectIds)){
    set("projectId", projectId)
    code(2)
    for(let bucketName of iterate(template.buckets)){
      set("bucketName", bucketName)
      code(3)
    }
  }

self-contained: true

code:
  - engine:
      - sh
      - bash
    source: |
      gcloud projects list --format="json(projectId)"

    extractors:
      - type: json
        name: projectIds
        internal: true
        json:
          - '.[].projectId'

  - engine:
      - sh
      - bash
    source: |
      gsutil ls -p $projectId | jq -R . | jq -s .

    extractors:
      - type: json
        name: buckets
        internal: true
        json:
          - '.[]'

  - engine:
      - sh
      - bash
    source: |
      gsutil uniformbucketlevelaccess get $bucketName

    matchers:
      - type: word
        words:
          - 'Enabled: False'

    extractors:
      - type: dsl
        dsl:
          - '"Uniform bucket-level access is not enabled for the bucket " + bucketName + " in project " + projectId'
# digest: 4b0a00483046022100845f2d4f9de7940afaef8f698902d248f08564b4b022415ee008cbfb9826018d022100ccf10d8da665628c3b477b8d3279d590cde4f2dc0dcb6155c5c629ace7bdb05f:922c64590222798bb761d5b6d8e72950