id: gitlab-detect

info:
  name: Gitlab 登录面板识别（暴露面识别）
  author: ehsahil
  severity: info
  description: 该模板通过识别 Gitlab 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  classification:
    cvss-metrics: CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
    cpe: cpe:2.3:a:gitlab:gitlab:*:*:*:*:community:*:*:*
  metadata:
    max-request: 1
    vendor: gitlab
    product: gitlab
    shodan-query:
      - http.title:"GitLab"
      - cpe:"cpe:2.3:a:gitlab:gitlab"
      - http.title:"gitlab"
    fofa-query: title="gitlab"
    google-query: intitle:"gitlab"
  tags: panel,gitlab,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/users/sign_in"

    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        words:
          - 'GitLab'
          - 'https://about.gitlab.com'
        condition: and

      - type: status
        status:
          - 200
# digest: 4a0a00473045022100db79bab740dfd87f093955290dd389329451c3a7f7bbcb975ad33de4f8479dfe022021e6eecc83f16d36eb144808a25100471a827e2758fa18d7c7a8d0f17861bbba:922c64590222798bb761d5b6d8e72950