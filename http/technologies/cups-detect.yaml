id: cups-detect

info:
  name: CUPS 指纹识别（指纹识别）
  author: rxerium
  severity: info
  description: |
    用于识别目标是否部署 CUPS 并提取相关版本或构建信息，模板会结合页面正文、状态码、特定路径或已知端点和版本或标识提取结果进行指纹匹配。
  metadata:
    verified: true
    max-request: 1
    shodan-query: product:"CUPS (IPP)"
  tags: cups,tech,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "Web Interface is Disabled - CUPS"
          - "Forbidden - CUPS"
          - "Server: CUPS"
        condition: or

      - type: status
        status:
          - 200
          - 404
          - 403
        condition: or

    extractors:
      - type: regex
        part: body
        name: version
        regex:
          - 'CUPS v([0-9.]+)>'
# digest: 4a0a0047304502205538cb5c63faa8303f206b5e0176b5ee2ce1d3ddd946a65270e67bdc3986fc8c02210091bc4656f5d69662a08a2901183558d250d5fbcfe02f4f9faafe3d01ea69f2b6:922c64590222798bb761d5b6d8e72950