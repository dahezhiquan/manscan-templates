id: hyperdx-panel

info:
  name: HyperDX 面板识别（指纹识别）
  author: righettod
  severity: info
  description: |
    该模板通过识别 HyperDX 面板、控制台或管理界面的页面特征，判断目标是否暴露了对应服务入口。若命中，说明该后台或运维界面对外可访问，可用于资产识别与暴露面排查。
  reference:
    - https://github.com/hyperdxio/hyperdx
    - https://www.hyperdx.io/
  metadata:
    max-request: 1
    verified: true
    shodan-query: html:"hyperdx"
  tags: panel,hyperdx,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/search"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_any(to_lower(body), ">hyperdx</span>", ">loading hyperdx<")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:

          - '(?i)"version":"([0-9.]+)"'
# digest: 490a004630440220606f076292c3d6a7763a92aa4813fe713362f71d9f6298ba91e0edb0e7cde58f02201bfac65291387eacfb193f728d83550c03ff557e82a622b417c2efbd8ee529c0:922c64590222798bb761d5b6d8e72950