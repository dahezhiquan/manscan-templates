id: umbraco-installer

info:
  name: Umbraco Install 暴露
  author: DhiyaneshDk,stvnhrlnd
  severity: high
  description: |
    检测到 Umbraco Install 暴露。该页面属于安装、初始化或配置向导流程，若对外暴露，攻击者可能完成初始化、创建管理员账户或重新配置目标系统。
  classification:
    cpe: cpe:2.3:a:umbraco:umbraco_cms:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: umbraco
    product: umbraco_cms
    shodan-query: title:"Install Umbraco"
  tags: misconfig,umbraco,install,oss,vuln

http:
  - method: GET
    path:
      - '{{BaseURL}}/install'
      - "{{BaseURL}}/umbraco/management/api/v1/server/status"


    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: dsl
        dsl:
          - 'contains_all(body, "Install Umbraco", "umbracoInstallPageBody")'
          - 'contains_all(body, "serverStatus", "Install")'
        condition: or

      - type: dsl
        dsl:
          - 'contains_any(header, "text/html", "application/json")'
          - 'status_code == 200 || status_code == 503'
        condition: and
# digest: 4b0a00483046022100aaf2548be45eb9da36cbc769e549487662bcd5dae1d264099e00748f6538f7bb022100e77ba7c82307677055920ee37ab0199d1ad6400c15034c67a4763f3499da8f2f:922c64590222798bb761d5b6d8e72950