id: motd-file-permissions-check

info:
  name: 确保 /etc/motd 的访问权限已正确配置（本地基线扫描）
  author: Th3l0newolf
  severity: medium
  description: |-
    此模板检查 /etc/motd 是否具有安全的属主和权限。
  remediation: |-
    设置 `/etc/motd` 的属主和权限：
      sudo chown root:root /etc/motd
      sudo chmod 644 /etc/motd
  reference:
    - https://www.cisecurity.org/benchmark/ubuntu_linux
  tags: cis,linux,ubuntu,motd,permissions,audit
  metadata:
    verified: true

self-contained: true

code:
  - engine:
      - bash
    args:
      - "-c"
      - |
        if [ ! -e /etc/motd ]; then
          echo "[motd-file-permissions-check:Policy-Pass] [/etc/motd does not exist] [CIS_PASS] [medium]"
          exit 0
        fi

        mode=$(stat -Lc "%a" /etc/motd)
        uid=$(stat -Lc "%u" /etc/motd)
        gid=$(stat -Lc "%g" /etc/motd)

        if [ "$uid" -eq 0 ] && [ "$gid" -eq 0 ] && [ "$mode" -le 644 ]; then
          echo "[motd-file-permissions-check:Policy-Pass] [/etc/motd ownership and permissions are correctly configured] [CIS_PASS] [medium]"
        else
          echo "[motd-file-permissions-check:Policy-Fail] [/etc/motd ownership or permissions are incorrectly configured] [CIS_FAIL] [medium]"
        fi

    matchers:
      - type: word
        name: policy-pass
        words:
          - "Policy-Pass"

      - type: word
        name: policy-fail
        words:
          - "Policy-Fail"
# digest: 4a0a0047304502205337c124afb6ffea14ac939029a929394b67bfeadb6e6f0003cee0c40ffd1fa0022100b4febaa25daaaeb3a029834eeccccfdabb508014c26b9f131229f38f4c6b78b1:922c64590222798bb761d5b6d8e72950
