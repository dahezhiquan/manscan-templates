id: sentry-panel

info:
  name: Sentry 登录面板（暴露面识别）
  author: righettod
  severity: info
  description: |
    该模板通过识别 Sentry 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  reference:
    - https://sentry.io/
  classification:
    cpe: cpe:2.3:a:sentry:sentry:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: sentry
    product: sentry
    shodan-query:
      - http.title:"Login | Sentry"
      - http.title:"login | sentry"
    fofa-query: title="login | sentry"
    google-query: intitle:"login | sentry"
  tags: panel,sentry,login,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    redirects: true
    max-redirects: 3

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains(body, "/sentry/") && contains(body, "Login")'
          - 'regex("(?i)<title>[^<]*sentry[^<]*</title>", body)'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)"current":\s*"([0-9a-z.-]+)"'
# digest: 4b0a004830460221008bb3c66d6381523b0a0eaa32985566f168985f37f32515ddfe608c80b949139e022100c056685d8756922ca34869f912cdc70e5d507322fd4fe136d4b84ba6eb174330:922c64590222798bb761d5b6d8e72950