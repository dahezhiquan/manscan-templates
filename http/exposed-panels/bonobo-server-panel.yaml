id: bonobo-server-panel

info:
  name: Bonobo Git Server 登录面板识别（指纹识别）
  author: bhutch
  severity: info
  description: |
    该模板通过识别 Bonobo Git Server 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  classification:
    cpe: cpe:2.3:a:bonobogitserver:bonobo_git_server:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 2
    vendor: bonobogitserver
    product: bonobo_git_server
    shodan-query: http.favicon.hash:-219625874
  tags: panel,bonobo,git,login,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/git"

    host-redirects: true
    max-redirects: 2
    stop-at-first-match: true

    matchers-condition: and
    matchers:
      - type: word
        words:
          - '<a href="https://github.com/jakubgarfield/Bonobo-Git-Server">'

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)bonobo git server \((.*)\)'
# digest: 4b0a00483046022100a6d04d30775a9837702a750a0114882f79c2613d46a73bb22ca0fd90bb23486b022100a15e42d44ab5c5f5194d12e9fcd2963ab15473196291cc6ab2b48455f00907dd:922c64590222798bb761d5b6d8e72950