id: umami-panel

info:
  name: Umami 面板识别（指纹识别）
  author: userdehghani
  severity: info
  description: |
    该模板通过识别 Umami 面板、控制台或管理界面的页面特征，判断目标是否暴露了对应服务入口。若命中，说明该后台或运维界面对外可访问，可用于资产识别与暴露面排查。
  reference:
    - https://umami.is/
    - https://umami.is/docs
  metadata:
    verified: true
    max-request: 3
    shodan-query: http.favicon.hash:-130447705
  tags: panel,umami,login,detect,discovery

flow: http(1) && http(2)

http:
  - method: GET
    path:
      - "{{BaseURL}}/login"
      - "{{BaseURL}}/favicon.ico"

    stop-at-first-match: true
    matchers:
      - type: dsl
        dsl:
          - 'status_code==200 && contains_any(tolower(body), "Login | umami", "umami</div>", "umami</h1>", "<title>umami - login")'
          - "status_code==200 && (\"-130447705\" == mmh3(base64_py(body)))"
        condition: or

  - method: GET
    path:
      - "{{BaseURL}}/~404"

    extractors:
      - type: regex
        part: body
        regex:
          - 'v(?P<version>\d+\.\d+\.\d+)'
# digest: 4b0a00483046022100e6cfb6fd3783458a53a029bf1c588f5bf798848d18a1fc49f8f923c4b43b72c20221008e4c5b4578cf31cf03cb5d05a4fcf992d435ba7c13f290a2bcc9d5e3d3823fa2:922c64590222798bb761d5b6d8e72950