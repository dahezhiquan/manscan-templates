id: solr-panel-exposure

info:
  name: Apache Solr Admin 面板识别（暴露面识别）
  author: pdteam
  severity: info
  description: 该模板用于识别 Apache Solr Admin 是否存在对外暴露的管理、服务或状态界面。若命中，说明目标公开了相关功能入口，可作为资产盘点与攻击面收敛的依据。
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
    cpe: cpe:2.3:a:apache:solr:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 2
    vendor: apache
    product: solr
    shodan-query:
      - http.title:"Solr Admin"
      - http.title:"apache solr"
      - cpe:"cpe:2.3:a:apache:solr"
      - http.title:"solr admin"
    fofa-query:
      - title="solr admin"
      - title="apache solr"
    google-query:
      - intitle:"apache solr"
      - intitle:"solr admin"
  tags: panel,solr,apache,admin,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}/solr/'
      - '{{BaseURL}}'

    stop-at-first-match: true

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "<title>Solr Admin</title>"

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - 'favicon\.ico\?_=([0-9.]+)'
# digest: 4a0a0047304502205dd9acb2c6cb51cb7d7b7ddf19ac947fdbae3ea918c06c41674020372ea75695022100cf8efce80dd2498f321ff4f72e9858c464b362ec311e04b1ea82e40011f7311a:922c64590222798bb761d5b6d8e72950