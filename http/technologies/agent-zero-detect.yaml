id: agent-zero-detect

info:
  name: Agent-Zero 应用指纹识别（指纹识别）
  author: 0x_Akoko
  severity: info
  description: |
    用于识别目标是否部署 Agent-Zero 应用 并提取相关版本或构建信息，模板会结合页面正文、特定路径或已知端点和版本或标识提取结果进行指纹匹配。
  reference:
    - https://github.com/agent0ai/agent-zero
  metadata:
    verified: true
    max-request: 1
  tags: agent-zero,detect,tech,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains(body, "Agent Zero") && contains(body, "a0version")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - 'v([0-9]+\.[0-9]+\.[0-9]+(?:\.[0-9]+)?)'
# digest: 4a0a004730450221009ab1f1307b1ba8c25b691736e4058a9ad42421cf4c627dafeea1f7e47d03e65002204631b419548d18f586cbbac69f0d5160e7cffc6c70b4d117fb9f77436e3cf020:922c64590222798bb761d5b6d8e72950