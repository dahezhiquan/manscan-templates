id: auth-json

info:
  name: Auth.JSON 文件泄露（暴露面识别）
  author: DhiyaneshDk
  severity: high
  description: 检测到对外可访问的 Auth.JSON 文件。相关文件可能泄露项目结构、依赖关系、部署细节、敏感参数或其他不应公开的内部信息。
  metadata:
    verified: true
    max-request: 1
    shodan-query: html:"auth.json"
  tags: devops,exposure,files,auth,config,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}/auth.json"

    matchers-condition: or
    matchers:
      - type: word
        part: body
        words:
          - '"http-basic": {'
          - '"username":'
          - '"password":'
        condition: and

      - type: word
        part: body
        words:
          - '"github-oauth": {'
          - '"github.com":'
        condition: and

      - type: word
        part: body
        words:
          - '"bitbucket-oauth":'
          - '"consumer-key":'
          - '"consumer-secret":'
        condition: and
# digest: 4a0a0047304502200a78edc4bf6db34869337c785646dff584f09728075cbe10f54ee9a6f828dd26022100a3a9ae18fd5346c97315de76ca7536c2c7a1869b13c4eb0fed9bd9732c970620:922c64590222798bb761d5b6d8e72950