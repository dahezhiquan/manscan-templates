id: mcp-inspector-detect

info:
  name: MCP Inspector 指纹识别（指纹识别）
  author: pussycat0x
  severity: info
  description: |
    用于识别目标是否部署 MCP Inspector 并提取相关版本或构建信息，模板会结合页面标题、页面正文、特定路径或已知端点和版本或标识提取结果进行指纹匹配。
  reference:
    - https://github.com/modelcontextprotocol/inspector
  metadata:
    verified: true
    fofa-query: title="MCP Inspector"
  tags: tech,mcp,anthropic,discovery,ai

flow: http(1) && http(2)

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    host-redirects: true
    max-redirects: 2
    matchers:
      - type: dsl
        dsl:
          - status_code == 200
          - contains(body,"<title>MCP Inspector")
        condition: and
        internal: true

    extractors:
      - type: regex
        internal: true
        name: js
        group: 1
        part: body
        regex:
          - 'src="([^"]+\.js)"'

  - method: GET
    path:
      - "{{BaseURL}}{{js}}"

    matchers:
      - type: dsl
        dsl:
          - status_code == 200

    extractors:
      - type: regex
        name: version
        group: 1
        part: body
        regex:
          - 'const\s+version\s*=\s*"([0-9]+\.[0-9]+\.[0-9]+)'
# digest: 4a0a00473045022100badd3a5ad781659ce643667d38860917b037f42719ec3b421349ae3e0cd0ff57022009c4a9dc40627073477b41e44c007ec4c12f9b4c70d3562c6e89fffc7945cffc:922c64590222798bb761d5b6d8e72950