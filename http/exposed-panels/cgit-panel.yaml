id: cgit-panel

info:
  name: CGIT 识别（暴露面识别）
  author: tess,righettod
  severity: info
  description: 该模板通过识别 CGIT 面板、控制台或管理界面的页面特征，判断目标是否暴露了对应服务入口。若命中，说明该后台或运维界面对外可访问，可用于资产识别与暴露面排查。
  reference:
    - https://git.zx2c4.com/cgit/about/
  classification:
    cvss-metrics: CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
    cpe: cpe:2.3:a:cgit_project:cgit:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: cgit_project
    product: cgit
    shodan-query:
      - http.title:"Git repository browser"
      - http.title:"git repository browser"
    fofa-query: title="git repository browser"
    google-query: intitle:"git repository browser"
  tags: panel,git,cgit_project,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_all(to_lower(body), "git repository browser", "cgit")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)<meta\s+name=.?generator.?\s+content=.?([a-z0-9\s.]+).?'
# digest: 4a0a00473045022100f628cfd1117755e21e89f353635ec9e98c0fa2b49963c2d1a6c3315789d7bd9c02203d330ab9b4b180099adc53f754f2893f2d1ca81870c3672efd04bf895e71691d:922c64590222798bb761d5b6d8e72950