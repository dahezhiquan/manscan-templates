id: repetier-server-panel

info:
  name: Repetier Server 面板识别（暴露面识别）
  author: ritikchaddha
  severity: info
  description: |
    该模板通过识别 Repetier Server 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  classification:
    cpe: cpe:2.3:a:repetier-server:repetier-server:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    vendor: repetier-server
    product: repetier-server
    shodan-query:
      - title:"Repetier-Server"
      - http.title:"repetier-server"
    fofa-query: title="repetier-server"
    google-query: intitle:"repetier-server"
  tags: panel,repetier,detect,repetier-server,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/#!/login"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - 'content="Repetier-Server'
          - 'About Repetier-Server'
        condition: or

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        group: 2
        regex:
          - 'Repetier\-Server ([A-Za-z]+) ([0-9.]+)'
# digest: 4b0a00483046022100da6be1a34e062c434caec904c5e046ea6025a19f4a970c960aa1459a5c331ccb02210081d227c8e3f024308e151d68640129d33673db20d01382bed3ea8cf4ba1a318c:922c64590222798bb761d5b6d8e72950