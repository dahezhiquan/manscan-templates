id: activemq-workflow

info:
  name: ActiveMQ 安全检查工作流（工作流）
  author: daffainfo
  description: 该工作流会在给定目标上运行所有与 ActiveMQ 相关的 nuclei 模板。
workflows:
  - template: http/exposed-panels/activemq-panel.yaml
    subtemplates:
      - tags: activemq
