id: rcdevs-webadm-panel

info:
  name: RCDevs WebADM 面板识别（暴露面识别）
  author: righettod
  severity: info
  description: |
    该模板通过识别 RCDevs WebADM 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  reference:
    - https://www.rcdevs.com/solutions/
  metadata:
    verified: true
    max-request: 4
    shodan-query: http.html:"WebADM"
  tags: panel,rcdevs,webadm,login,detect,discovery

http:
  - method: GET
    path:
      - '{{BaseURL}}'
      - '{{BaseURL}}/webapps/index.php'
      - '{{BaseURL}}/websrvs/index.php'
      - '{{BaseURL}}/admin/login_uid.php'

    stop-at-first-match: true

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - 'WebADM'
          - 'RCDevs Security'
          - 'www.rcdevs.com'
        condition: and
        case-insensitive: true

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)webadm\s+(free|enterprise)\s+edition'
# digest: 4a0a0047304502205e0e9a4aaefd901113456305cfe03446b854f96b5ccca45c5253c7f8aa998864022100b23908b23e6586511b3d1963d06235b73be262ef26fd425ca842fa770d0e55d8:922c64590222798bb761d5b6d8e72950