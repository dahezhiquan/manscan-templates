id: tixeo-panel

info:
  name: Tixeo 登录面板识别（暴露面识别）
  author: righettod
  severity: info
  description: |
    该模板通过识别 Tixeo 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  reference:
    - https://www.tixeo.com/en/
  metadata:
    verified: true
    max-request: 3
    shodan-query: "http.title:\"tixeo\""
  tags: panel,tixeo,login,detect,discovery
http:
  - method: GET
    path:
      - "{{BaseURL}}/meet/services/json/v1/settings"
      - "{{BaseURL}}/meet/login.html"
      - "{{BaseURL}}/meet/"

    stop-at-first-match: true

    matchers:
      - type: dsl
        dsl:
          - 'contains(header, "Tixeo")'
          - 'contains_any(to_lower(body), "tixeo-button", "tixeoclient")'
        condition: or

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '"applicationVersion"\s*:\s*"([0-9.]+)"'
# digest: 4b0a0048304602210083d285c6bd9a985151fe0a817a92306399a5c2aa5fc630e6303478a181947dde022100f99c0f1dac2aa8d285185c0d61c1208044b571065b5749a52d7af7488c1f61df:922c64590222798bb761d5b6d8e72950