id: gitlab-workflow

info:
  name: GitLab 安全检查工作流（工作流）
  author: pdteam
  description: 该工作流会在给定目标上运行所有与 GitLab 相关的 nuclei 模板。
workflows:
  - template: http/exposed-panels/gitlab-detect.yaml
    subtemplates:
      - tags: gitlab
