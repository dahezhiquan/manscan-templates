id: gcloud-pubsub-publicly-accessible

info:
  name: 公开可访问 Pub/Sub Topics（云安全）
  author: princechaddha
  severity: high
  description: 该规则用于检查 GCP pubsub 相关资源是否存在公开暴露、未受限访问或匿名可访问等高风险配置，避免敏感数据、管理接口或网络服务直接暴露在公网。
  impact: |
    - 公开暴露的资源可能被未授权访问、枚举、下载或恶意利用。
    - 一旦涉及管理接口、对象存储或数据库服务，可能进一步导致数据泄露、权限滥用或业务中断。
  remediation: |
    - 收敛公网暴露面，仅允许受信任网络、IP 范围或必要身份访问相关资源。
    - 对对象存储、负载均衡、数据库或控制台接口启用更严格的访问控制与网络隔离策略。
  reference:
    - https://cloud.google.com/pubsub/docs/access-control
  tags: cloud,devops,gcp,gcloud,google-cloud-pubsub,gcp-cloud-config

flow: |
  code(1)
  for(let projectId of iterate(template.projectIds)){
    set("projectId", projectId)
    code(2)
    for(let topic of iterate(template.pubsubTopics)){
      set("pubsubTopic", topic)
      code(3)
    }
  }

self-contained: true

code:
  - engine:
      - sh
      - bash
    source: |
      gcloud projects list --format="json(projectId)"

    extractors:
      - type: json
        name: projectIds
        internal: true
        json:
          - '.[].projectId'

  - engine:
      - sh
      - bash
    source: |
      gcloud pubsub topics list --project $projectId --format="json(name)"

    extractors:
      - type: json
        name: pubsubTopics
        internal: true
        json:
          - '.[].name'

  - engine:
      - sh
      - bash
    source: |
      gcloud pubsub topics get-iam-policy $pubsubTopic --format=json | jq '.bindings[].members[]'

    matchers:
      - type: word
        words:
          - 'allUsers'
          - 'allAuthenticatedUsers'
        condition: or

    extractors:
      - type: dsl
        dsl:
          - '"Publicly Accessible Pub/Sub Topic found: " + pubsubTopic + " in project: " + projectId'
# digest: 490a0046304402204083a725bc96738a4a1fb931e117c8bbfaaa3424b5337ae5485a822780d674c102204ed357c9b0b732cc5cd11dbc830f3798e7a4f3d6a73aada6c904f7629b0b1fdf:922c64590222798bb761d5b6d8e72950