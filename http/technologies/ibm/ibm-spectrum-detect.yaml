id: ibm-spectrum-detect

info:
  name: IBM Spectrum 指纹识别（指纹识别）
  author: righettod
  severity: info
  description: |
    用于识别目标是否部署 IBM Spectrum 并提取相关版本或构建信息，模板会结合页面标题、页面正文、特定路径或已知端点和版本或标识提取结果进行指纹匹配。
  reference:
    - https://www.ibm.com/docs/en/products?filter=spectrum
  metadata:
    max-request: 1
    verified: true
    shodan-query: http.title:"IBM Spectrum"
  tags: tech,ibm,spectrum,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/"
      - "{{BaseURL}}/BACLIENT"
      - "{{BaseURL}}/JNLP"

    stop-at-first-match: true
    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_all(to_lower(body), "ibm spectrum", "com.ibm.")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)"guiVersion"\s*:\s*"([0-9.\-]+)"'
          - '(?i)name="?version"?\s+value="?([0-9.\-]+)"?'
# digest: 4a0a0047304502210084dd8f1a601e0ac0846b02762c9e006b3c02c3fd53bf6ae4684909007f82478d02202aa3a9aa60f1356ddc79b88d67bd74bc6b6fcb2caeb179b791d5ba4528c84595:922c64590222798bb761d5b6d8e72950