id: adfinity-panel

info:
  name: Adfinity 登录面板识别（暴露面识别）
  author: righettod
  severity: info
  description: |
    该模板通过识别 Adfinity 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  reference:
    - https://easi.net/en/solutions/adfinity
  metadata:
    max-request: 1
    verified: true
    shodan-query: http.html:"Adfinity"
  tags: panel,adfinity,login,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}/adfinity/login"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_any(to_lower(body), "adfinity web application", "adfinity web access", "csrf_adfinity_token")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)Version\s+Adfinity([0-9.]+)'
# digest: 490a0046304402206475108fbc9dc4a8da32f412b90408372317a46b136bce739c8687541523073f022018dafeef2b2b7689a425bb2203f3ae44c600cdec545d56981d3928b883085cc6:922c64590222798bb761d5b6d8e72950