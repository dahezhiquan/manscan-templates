id: magento-config-disclosure

info:
  name: Magento 配置 Panel 检测（暴露面识别）
  author: ptonewreckin,danigoland,geeknik
  severity: high
  description: |
    检测到对外可访问的 Magento 配置 Panel。这类配置资源可能包含部署细节、访问凭据、内部地址、组件版本或调试开关，公开暴露后会显著增加被侦察和后续利用的风险。
  reference:
    - https://github.com/ptonewreckin/cmsDetector/blob/master/signatures/magento.py
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N
    cvss-score: 7.5
    cwe-id: CWE-200
    cpe: cpe:2.3:a:magento:magento:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 3
    shodan-query: http.component:"Magento"
    product: magento
    vendor: magento
  tags: magento,exposure,credential,config,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}/app/etc/local.xml"
      - "{{BaseURL}}/app/etc/local.xml.additional"
      - "{{BaseURL}}/store/app/etc/local.xml"

    stop-at-first-match: true

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "* Magento"
          - "<dbname>"
        condition: and

      - type: word
        part: header
        words:
          - "application/xml"

      - type: status
        status:
          - 200

    extractors:
      - type: xpath
        part: body
        xpath:
          - "/config/global/resources/default_setup/connection/host"
          - "/config/global/resources/default_setup/connection/username"
          - "/config/global/resources/default_setup/connection/password"
          - "/config/global/resources/default_setup/connection/dbname"
# digest: 4b0a00483046022100d231896c4e621ba4ea60c30d1f7688d2fb1dae8354a3e2fdc42a8f1ed300bbc1022100f53d235cb7566c568b8137a28ba6c346c5465d51c734c77bb4acf5fb55f46b95:922c64590222798bb761d5b6d8e72950