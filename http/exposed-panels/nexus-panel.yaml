id: nexus-panel

info:
  name: Nexus 登录面板识别（暴露面识别）
  author: righettod
  severity: info
  description: |
    该模板通过识别 Nexus 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  reference:
    - https://www.sonatype.com/products/sonatype-nexus-repository
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.title:"Sonatype Nexus Repository"
  tags: panel,nexus,login,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_any(to_lower(body), "<title>sonatype nexus repository", "content=\"sonatype nexus repository", "nexus-coreui-bundle")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '_v=([0-9\.\-]+)'
# digest: 4a0a00473045022100c98279abc78836a033800bbb0668ffb9d75e905fbcb3bfb11935c80882b769ba02206a91df2acc6b23735f5c66e9dce87514af86d4221abf72c671adc0d3c5fd1fa6:922c64590222798bb761d5b6d8e72950