id: redis-config

info:
  name: Redis 配置文件检测（暴露面识别）
  author: geeknik
  severity: medium
  description: 检测到对外可访问的 Redis 配置文件。这类配置资源可能包含部署细节、访问凭据、内部地址、组件版本或调试开关，公开暴露后会显著增加被侦察和后续利用的风险。
  reference:
    - https://redis.io/docs/manual/config/
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N
    cvss-score: 5.3
    cwe-id: CWE-200
    cpe: cpe:2.3:a:redis:redis:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 1
    shodan-query: html:"redis.conf"
    product: redis
    vendor: redis
  tags: redis,exposure,config,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}/redis.conf"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "bind"
          - "protected-mode"
          - "port "
        condition: and

      - type: word
        part: header
        words:
          - "application/octet-stream"

      - type: status
        status:
          - 200
# digest: 4b0a00483046022100b2ad2fd273fe5115ffb32a28f8f3fc0fee7d9a4f6bf455fa4042dc935593f5570221009b423c409f265d9df09c6aa370d0e995f505a658c472554d2f9087639dc36a60:922c64590222798bb761d5b6d8e72950