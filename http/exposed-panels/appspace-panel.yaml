id: appspace-panel

info:
  name: Appspace 登录面板识别（暴露面识别）
  author: ritikchaddha
  severity: info
  description: 该模板通过识别 Appspace 登录入口的页面标题、特征文本或重定向行为，判断目标是否暴露了对应的认证界面。若命中，说明该登录面板对外可访问，可用于暴露面梳理与后续安全评估。
  reference:
    - https://www.appspace.com/
  classification:
    cpe: cpe:2.3:a:appspace:appspace:*:*:*:*:*:*:*:*
  metadata:
    verified: true
    max-request: 3
    vendor: appspace
    product: appspace
    shodan-query:
      - title:"Appspace"
      - http.title:"appspace"
    fofa-query: title="appspace"
    google-query: intitle:"appspace"
  tags: appspace,panel,detect,discovery

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/app/login.aspx"
      - "{{BaseURL}}/signin/#!/login?returnUrl=%2Fapp%2Fdefault.aspx"

    stop-at-first-match: true
    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '.appspace-ui'
          - 'appspace-logo'
          - '<title>Appspace</title>'
          - 'class="btnSSOAppspace'
          - 'Sign in to Appspace Core'
        condition: or
        case-insensitive: true

      - type: status
        status:
          - 200
# digest: 4a0a004730450220023dc0047646baa7f3a7a26b67ca9de3254dc2be4ec8974ad61d53883702c853022100824d2acd3810ab8902ffd05f6c915e233e589d3c027a42abc46a9247755ab0a6:922c64590222798bb761d5b6d8e72950